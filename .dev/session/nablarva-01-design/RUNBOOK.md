---
goal: Choose a build-ready first Nablarva increment that reduces human session relay while preserving independent seats and human authority.
gate: Majkee records GO or STOP on Oraculum's evidence-backed audit of Cartan's first-brick Nablarva design and acceptance test.
head_note: "cSharp — Oraculum authored this RUNBOOK and stays live through the whole arc as navigator and status_owner; it may delegate every body of work and receive only navigation + test parts; it closes the session if it can."
participant_1: [oraculum, {brand: claude-code-cli, model: fable, effort: operator-selected}, hruzam-120922]
participant_2: [cartan, {brand: codex-cli, model: astra, effort: session-configured}, hruzam-120922, resident]
participant_3: [majkee, {brand: human, model: not-applicable, effort: not-applicable}, hruzam-120922]
status_owner: oraculum
standing_witness: "cartan — tests head coordination and STATUS claims as paths (RETURN §4); never accepts its own design"
schema_note: "raw.guides/runbook/GUIDE.md verified 2026-09-05 · res/csharp-head-protocol.md gaveled 2026-09-04 · res/fanout-turns.md gaveled 2026-09-05 · instrument column per res/cross-vendor-seat.md (DRAFT, declarative only)"
adopted_from: "/home/hruzam/unikuklatrix/nablarva/meshup/nablarva-01-design/RUNBOOK.md — Cartan candidate 2026-09-12, countersigned by Oraculum in chat the same day; after this promotion that copy is a template, never a state store"
adopted_on: 2026-09-12
repaired: "2026-09-12, after RETURN 01 §4 and the Janus pass — four factual sentences in §Why this session and §Known holds (relay figures, native/reuse wording, L6 scope, board freeze); gate, roles, prompts unchanged in meaning; recorded as curvature in _bus/01.oraculum.verdict.md"
gloss: ../GLOSS.nablarva.md
---

# RUNBOOK — nablarva-01-design

> Read once. Position lives only in `STATUS.md`. Model labels are majkee's allocation, not
> attestations or executable IDs. Seats in `_bus/` filenames: `oraculum`, `cartan`.

## Why this session

- Measured pain: the operator is the copy/paste middleware between independent full CLI sessions.
  The only prior telemetry (runbook-upgrade head thread) counts 86–88 operator-message records
  depending on boundary, per its owner VERDICT, which also rejects counting zero-tool replies as
  relays. That is context, not a baseline; this arc's sender-side baseline is unknown until counted.
  Vendors ship transport; nobody ships the seam between runtimes or the discipline on it.
- Golden rule, small-scope inline: no installed feature binds a RUNBOOK seat to a live session
  across vendors. The three checks of `res/research.md` (wheel · overengineering · vendor-harness)
  were addressed at verdict level by the a-symmetry brief + countersign pair — no existing wheel,
  slice not engine, vendors ship transport not the seam — cited as research substrate, not as a
  `-00-research` VERDICT. The mechanism-level questions — live attachment, readiness, usage path —
  are open and are this design's job. If the increment grows into a standalone system, stop and
  open `nablarva-00-research` instead of widening this gate.
- Native change that retires the delta: a vendor-native durable cross-session join preserving
  equivalent sovereign receipts (`res/fanout-turns.md` §Boundary and retirement) plus native
  cross-vendor session addressing.
- Design only. A build is a numbered sibling with its own observable gate.

## Scope and gate

Design one same-host, one-bed extension: operator-selected full sessions attached to the run's
head, explicit endpoint bindings, and a graduated path from preparation to one demonstrated,
correlated request/response pipe. Nablarva is the product; this names no second relay product
and decides no semi-IDE.

Deliverable: Cartan's design as **immutable revisions** `raw/design.r<N>.md` — never edited; a
correction is `r<N+1>` in a new cycle — including the first increment's predeclared acceptance
test, a checked six-field RETURN, and Oraculum's independent VERDICT naming the audited revision.
GO or STOP is recorded against that exact revision file. REVISE continues the gate; GO or STOP
closes it once recorded. GO requires material lock conflicts and prerequisite holds resolved or
the dependent mechanism excluded. GO approves the bounded design, not a build, a live-send trial,
or a deployment.

## Shared rules and ownership

- Oraculum: first in, last out — navigator, sole STATUS writer, auditor of Cartan's technical
  work. Cartan: primary technical designer and later integration owner, not a compatibility
  reviewer. Oraculum audits without fixing; a revision returns through a new cycle. If Oraculum
  substantially co-authors the technical body, a named uninvolved reviewer witnesses it before
  acceptance; the independence limit is recorded, never self-confirmed.
- Two tiers. Full-session exchanges use `_bus/` (POINT → RETURN → VERDICT). In-window helpers on
  either side (Oraculum's krakens, Cartan's native helpers) are Tier B: bounded reads, melts,
  scratch — their claims are checked and republished by the owning seat, they never advance
  STATUS, never impersonate a full seat, never get a BUS cycle each.
- Cartan's STATUS-claims check lives in its RETURN §4 (each claim tested as a path) — no
  ceremony cycle for witnessing. Cartan witnesses coordination, never its own acceptance.
- Majkee carries absolute paths between the two full sessions and holds every gavel. No automatic
  wake, live steering, or background watcher is authorized here. One STATUS, content-sensitive
  recovery, immutable handed-off receipts, no fourth BUS kind.

## Known holds and write boundaries

- Activation scope: this bed and one router row in `/home/hruzam/unikuklatrix/nablarva/.dev/session/pulse.md`.
  Oraculum owns `RUNBOOK.md`, `STATUS.md`, the router row, its POINTs and VERDICTs. Cartan owns
  `raw/design.r*.md` and its RETURNs. Any other file needs a named owner before it exists.
- The brief is Cartan's and stays where it was authored (append-only pen):
  `/home/hruzam/unikuklatrix/nablarva/meshup/nablarva-01-design/brief.md`. Never edited here.
- **Surgical table, scoped.** This increment extends an existing ia-sync tool, so its source is
  composed in `/home/hruzam/ia-sync/zsh/session/` and spread by `deploy.sh` (session-browser
  contract); harness surfaces, if any, in `/home/hruzam/ia-sync/claude/` or `codex/`. Nothing is
  written to live `~/.config`, `~/.claude`, `~/.codex`. Flag L6's distinction stands for future
  organs — experimental shapes may build in nablarva and promote by reviewed merge — and this
  hold does not decide their home. Facts about ia-sync surfaces are gathered there, read-only,
  by spawns (@eagle, @epoch) or by Cartan's helpers.
- `/home/hruzam/ia-sync/zsh/session/runbook.py` and the open bed
  `/home/hruzam/ia-sync/.dev/session/runbook-tool-00/` (d-done, home carry owed) are read-only
  references. The browser's gaveled law is read + land + drain. Trajectory is the consulted tool
  owner, not a woken participant; extension timing is majkee's + Trajectory's call.
- Flag L3/L4/L6/L9′/L12 bind. L4 records the death of send-keys as the *content-injection leg*; a
  path-only doorbell was never killed but has no explicit carve-out either. Name the distinction;
  cite the exact lock; request a gavel before choosing any mechanism that needs an exception.
  Later prose is not a gavel.
- Presence board: contract gaveled 2026-09-09 and client built; advisory only, v1 carries no
  session endpoint, `attachment_id` is not an endpoint. Identity ontology is unsettled — do not
  fork a second scheme; a binding record is a local handle, not a global identity.
- No terminal injection, launch, remote transport, runtime/config install, deploy, auth access,
  repository cleanup, commit, push, or prune. Do not read `/home/hruzam/unikuklatrix/nablarva/.hlm/`.
  Preserve other sessions' dirt and sealed work. Do not move interactive work onto an API-billed
  path — `claude -p` and `codex exec` are not the living session.
- Out of this gate: higher-mathematics research, global role identity, unattended operation,
  cross-host delivery, a general IDE/orchestrator, gavel docket items 1/5/6. Not reopened by analogy.

## prompt-0 — oraculum / cSharp

You are `oraculum`, the independent full Claude Code CLI session majkee selected. Read, in order:
`/home/hruzam/unikuklatrix/nablarva/AGENTS.md` → `.dev/session/flag.md` → `.dev/session/pulse.md`
→ `PROJECT.yaml` → this RUNBOOK → `STATUS.md` here → the brief at its meshup path and the
opening route it names → the shared law below. On recovery, STATUS first.

Delegate one coherent POINT 01 to the existing `cartan` session (never a spawned substitute):
technical design, native/reuse evidence, predeclared acceptance test, manual baseline count.
Name exact write paths, prohibited operations, done-when. Write POINT and STATUS, then hand majkee
one absolute path. On RETURN, inspect the revision and every cited artifact; run one read-only
adversarial pass (@janus) before writing your VERDICT; do not repair the design. Keep STATUS a
short snapshot — one `next:`, no history. Reconcile disk before any resumed dispatch. Ask majkee
for GO or STOP only when the package is ready; record the exact decision and revision. Stay
through closeout: keepers, experience transfer, requested — never assumed — commit/prune authority.
Executor grades: melts and wide reads → @field; ia-sync surface facts → @eagle / @epoch
(read-only on the surgical table); safe command reruns → @assay; adversarial pass → @janus;
no coding grade in this gate. Keep `../GLOSS.nablarva.md` current before any step the operator sits.

## prompt-1 — cartan / technical lead

You are `cartan`, majkee's existing independent Codex session, not Oraculum's child. Read the
project chain in prompt-0, then `/home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/RUNBOOK.md`,
`.../STATUS.md`, your brief at `/home/hruzam/unikuklatrix/nablarva/meshup/nablarva-01-design/brief.md`,
and the POINT majkee carries with its cited substrate. Resolve your declared seat first; a
mismatched `to:` or `return_to:` stops the cycle. Write `raw/design.r<N>.md` and one six-field
RETURN; never rewrite STATUS. Preserve disagreements; separate observed facts from proposals.

The design must make concrete, each as a named section:
1. **Boundary** — where endpoint binding and the activation act live. The browser feeds them; a
   sibling in the same `zsh/session/` scope is fine, a new product/daemon/framework is not.
   Source home for this extension is `/home/hruzam/ia-sync/zsh/session/` (surgical table), live
   `~/.config/zsh/session/` via `deploy.sh`.
2. **Binding record** — seat → {host, runtime, endpoint handle, bound_at}; host-local, beside the
   bed, gitignored, borrowed lifecycle (precedent: `tunnel.state.json`, `res/cross-vendor-seat.md`).
   Keep seat, bed, host, workspace, runtime, incarnation distinct; a title is a search aid, not
   identity. Explicit rebinding; missing/stale/ambiguous binding stops sending. Diverge only with reason.
3. **Correlation** — the exchange ID is the BUS POINT and its `return_to:`; the receipt is the
   RETURN at exactly that path. No second ledger. Mission pointer only — no composer/reminder.
4. **Native/reuse evidence, dated, from the installed interface**: does `codex queue` exist on
   the installed codex-cli, does it reach the *living interactive* session on the subscriber
   path; what Claude cross-session messaging exists on the installed version; Claude Stop hook
   (`last_assistant_message`, `session_id`, `transcript_path`) as completion signal; Codex
   app-server / `exec --json` as read-back. If both native activations exist, no L4 question
   arises; if not, state the doorbell-vs-injection distinction and draft the exception request.
   Costa Probe A (`meshup/grounded-composites/`) is designed-never-run precedent, not proof.
5. **Uncertainty after submission** — prepared / submitted / consumed / RETURN present / witness
   accepted / STATUS advanced are distinct events. Timeout is unknown, not permission to resend.
   No silent retargeting, no blind retry. Manual fallback stays.
6. **Layer map** — this brick lives on task evidence (RUNBOOK/STATUS/BUS), not the L3 room
   journal; docket 1/5/6 not pre-decided; no "room" vocabulary; say which "adapter" you mean;
   cite `05_DECISION_LEDGER §5.9 Session attachment` as the thread un-parked by operator direction.
7. **Acceptance test** — a future trial vs the current manual workflow with a **hand-counted
   baseline exchange first**; observable acceptance and stop conditions agreed before execution;
   count operator interventions; negative cases: wrong bed/seat, stale/replaced endpoint,
   busy/approval state, interruption after possible submission. Specified here, not run.
8. **Retirement** — the native change that makes the delta unnecessary.

Keep the first build bounded enough for independent audit.

## prompt-3 — majkee / carrier and gavel

Wake seats only from absolute POINT paths; carry paths, never content. Gavel L4 exceptions,
tool-extension timing (with Trajectory), and the final GO/STOP against the named revision. A chat
decision is preserved by Oraculum as a human decision, never as a fabricated receipt. Your
learning file for this part is `/home/hruzam/unikuklatrix/nablarva/.dev/session/GLOSS.nablarva.md`.

## References — shared law

- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` · `res/csharp-head-protocol.md` ·
  `res/fanout-turns.md` · `res/research.md` · `res/presence-board.md`
- `/home/hruzam/reposoma/raw.guides/status/GUIDE.md` · `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md`
- `/home/hruzam/reposoma/raw.guides/gloss/GUIDE.md` — the operator learning file
- `/home/hruzam/reposoma/raw.guides/session-browser/GUIDE.md` — the tool's own law
- `/home/hruzam/ia-sync/HANDSHAKE.md` §Delivery rule · `/home/hruzam/reposoma/raw.guides/tunnel/GUIDE.md`
- `res/token-economy.md` and `res/cross-vendor-seat.md` remain DRAFT; helper use follows majkee's allocation.

## What closes this gate

One design revision with acceptance test on disk, its RETURN verified by Oraculum's VERDICT
(curvature named, uncertainty disposed), and majkee's GO or STOP recorded against that revision
in STATUS and the closing VERDICT.

## Deliberately out of scope

Application code, a relay trial, deployment, BUS automation, scheduler, UI parser, cockpit, new
`_bus/` kinds, a second state store, global identity ontology, unattended or cross-host operation.
