---
cycle: "01"
from: oraculum
to: cartan
scope: "First-brick design revision r1 with predeclared acceptance test, dated native/reuse evidence from the installed interfaces, and the recipient-side count of this very exchange as the manual baseline."
paths:
  read:
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/RUNBOOK.md   # prompt-1 lists the eight required design sections
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/STATUS.md
    - /home/hruzam/unikuklatrix/nablarva/meshup/nablarva-01-design/brief.md            # your brief; its opening route
    - /home/hruzam/unikuklatrix/nablarva/meshup/a-symmetry-lightest/                    # three files
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/flag.md                          # L3 · L4 · L6 · L9′ · L12
    - /home/hruzam/reposoma/raw.guides/session-browser/GUIDE.md
    - /home/hruzam/reposoma/raw.guides/runbook/res/cross-vendor-seat.md                # handle-beside-bed precedent (DRAFT)
    - /home/hruzam/reposoma/raw.guides/tunnel/GUIDE.md
    - /home/hruzam/ia-sync/HANDSHAKE.md
    - /home/hruzam/ia-sync/zsh/session/runbook.py                                      # read-only reference
    - /home/hruzam/unikuklatrix/nablarva/meshup/grounded-composites/                   # Costa Probe A — designed, never run
    - /home/hruzam/unikuklatrix/nablarva/meshup/oraculum-basic-triangulation/05_DECISION_LEDGER_PARKED_BRANCHES_AND_FORKS.md  # §5.9 Session attachment
  write:
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/raw/design.r1.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/_bus/01.cartan.return.md
gates: "No STATUS/RUNBOOK/pulse write. No commit, push, prune, deploy, launch, terminal injection, tunnel open, seat wake, board edit, auth access, live-send experiment, or runtime/config change. Do not read /home/hruzam/unikuklatrix/nablarva/.hlm/. Native in-window helpers allowed; their claims are yours to check; no BUS cycle per helper. design.r1.md is immutable once handed off — a correction is r2 in a new cycle."
done_when: "raw/design.r1.md exists with the eight named sections of RUNBOOK prompt-1; 01.cartan.return.md exists with the six mandatory headings; §2 carries the dated installed-interface observations (exact commands + outcomes) behind every native/reuse claim; §3 carries your recipient-side baseline count; §4 tests every STATUS claim as a path and names every doc-vs-disk mismatch; §6 states what remains unverified; nothing else was written."
return_to: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/_bus/01.cartan.return.md
---

# POINT 01 — design r1, evidence, baseline

## 0. Resolve your seat before any work

You are `cartan`, majkee's existing independent Codex session. The operator declares your
endpoint as the Codex transcript whose id ends `c57d8`. If your thread id does not end so, or
you are a spawned child rather than that session, **stop before work** and write a RETURN whose
§4 reports the mismatch and whose other fields say `none` — that is the wrong-seat negative
case, exercised by hand. A title or window name is a locator, not identity.

## 1. Produce

- `raw/design.r1.md` — the eight sections named in RUNBOOK prompt-1, in that order, each a
  `##` heading: Boundary · Binding record · Correlation · Native/reuse evidence · Uncertainty
  after submission · Layer map · Acceptance test · Retirement. Observed facts and proposals
  visibly separated. Bounded enough for one auditor to check in one sitting.
- Native/reuse evidence is **dated and observed on the installed interfaces**, not recalled:
  codex-cli version; whether `codex queue` (or its current equivalent) exists and reaches the
  *living interactive* session on the subscriber path; Claude-side cross-session messaging on
  the installed version as far as your access allows — name the gap where it does not; the
  Claude Stop hook fields and Codex app-server / `exec --json` as completion/read-back signals.
  `claude -p` and `codex exec` are not the living session. If both native activations exist,
  say L4 is untouched; if not, state doorbell-vs-injection and draft the exception request for
  majkee — do not choose it.
- Acceptance test: acceptance and stop conditions stated before any run; operator-intervention
  count; the four negative cases (wrong bed/seat · stale/replaced endpoint · busy/approval
  state · interruption after possible submission); prepared / submitted / consumed / RETURN
  present / accepted / STATUS advanced kept distinct. Specified, not run.

## 2. Baseline — this cycle is the manual exchange

Count-before-build is satisfied by this exchange, not by a separate ceremony. In RETURN §3
record your **recipient side**: how you were told, what you had to do to locate and read this
POINT and its substrate, what context the file lacked that you had to reconstruct, wall-clock
from told-to-reading if you can observe it. The sender side (notice · locate · copy · switch ·
paste · explain · notice-return) is majkee's count and lands in STATUS as operator-reported.

## 3. RETURN shape

Six headings, all present; `none` explicit. §1 files; §2 exact commands and outcomes for every
verification-relevant observation; §3 evidence paths + baseline count; §4 mismatches — include
one line per STATUS claim tested as a path (`test -f`, `grep`), and every doc-vs-disk
disagreement named, never smoothed; §5 one bounded recommendation; §6 remaining uncertainty.
Bind frontmatter to `cycle: "01"`, `from: cartan`, this POINT's path. If `return_to` already
exists, stop and report; never overwrite. Give majkee the RETURN's absolute path when done.
