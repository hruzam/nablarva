updated: 2026-09-16
writer: Trajectory · Claude Code (ia-sync seat)
host: home (hruzam) — bed authored here; real probe host = wherever audio + phone live (unpinned)
worktree: /home/hruzam/unikuklatrix/nablarva · branch/HEAD unverified from ia-sync seat · this bed uncommitted
gate: (verbatim from RUNBOOK) One observed round-trip — a live CLI agent session emits a name-addressed spoken utterance AND a second, independent live CLI agent session detects it, recognizes its own name, and reacts in-session — OR a recorded verdict that the mechanism is infeasible, naming the specific blocker.
checkpoint: DRAFT bed exists (RUNBOOK.md + this STATUS.md). Nothing run. No physical substrate built. No pulse.md router row by design.
in_flight: none
recovery_probe: `ls /home/hruzam/unikuklatrix/nablarva/.dev/session/voice-relay-00-probe/` shows only RUNBOOK.md + STATUS.md → bed is a PARKED DRAFT, never started. If an evidence/pad file appears here or a pulse.md row names this slug, it has been promoted to live — re-read RUNBOOK before continuing.
holds:
  - PARKED (majkee, 2026-09-16): draft only — other project parts come first. Do NOT promote to an active session, do NOT add a pulse.md row, do NOT build the legs until majkee reopens.
  - Positional convention: a live bed sits in .dev/session/; a draft canonically sits on the bench (~/reposoma/_runbook/nablarva/voice-relay-00-probe/). Kept here at majkee's explicit path request. Move to bench if you want strict draft/live separation.
  - Constraint-1 reframed (majkee, 2026-09-16): the send-keys "death" (flag L4) is a process decision, not a wall — to be TESTED empirically in this probe (run uinput-inject path A vs file-as-inbox control B), not ruled beforehand. Target the test at send-keys' recorded cause.
next: majkee decides whether/when to reopen this probe. Until then it stays a parked draft.
expected: no action; the bed waits.
