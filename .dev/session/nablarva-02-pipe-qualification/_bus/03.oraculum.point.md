---
cycle: "03"
from: oraculum
to: cartan
corrects: "02"
scope: "Corrective P1 engineering: the private-app-server route for a disposable target (docs + exact commands), a working reader for that route (read-only: initialize · thread/list · thread/read), override rejection, extended offline tests. No server started, no TUI opened, no connection, no send."
paths:
  read:
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/02.oraculum.verdict.md   # judgment, host observations, curvature 4–7
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/STATUS.md                     # three-stage ladder; decision_rule
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/route.r1.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/qualification-plan.r1.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/pad.1-baseline-carry.md        # the sitting's raw surface (head-owned); read, never edit
  write:
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/route.r2.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/probe/qualify.py             # in-place revision; RETURN pins the new hash
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/probe/test_qualify.py        # in-place revision; RETURN pins the new hash
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/probe/fixtures/README.md     # only if the route changes fixture placement; else untouched
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/03.cartan.return.md
gates: "No `codex app-server` start of any kind, no `--remote` TUI launch, no connection to ANY socket (including /run/user/1000/codex-pocket-test.sock — unknown ownership, off limits), no `codex queue` beyond `--help`, no daemon verb, no hook/notify/config change, no credential read, no send. `inspect` may be fully implemented but must refuse to connect unless invoked with `--sock` pointing at a path that exists AND `--allow-connect <token>`; this cycle never supplies the token and the tests prove it. Scratch dir stays empty. No STATUS/RUNBOOK/pulse/GLOSS/PAD write. Do not read /home/hruzam/unikuklatrix/nablarva/.hlm/. Never write outside the listed paths."
done_when: "raw/route.r2.md names, with citations and unknowns marked: the private-server route (exact server command with a NEW private socket path under /run/user/1000/, exact TUI attach command, exact reader commands for initialize · thread/list · thread/read, and how `queue --remote` would address that server later — not executed); `qualify.py inspect` implements the documented Unix-socket transport (WebSocket + HTTP Upgrade, JSONL messages, stdlib only) with timeouts and byte bounds, plus `--allow-connect` gating; override rejection is implemented and tested (any of --dangerously-*, --approve-for-me, -a/--ask-for-approval never, --model, -p/--profile, -c on a target command line or inherited profile → BLOCKED); `python3 -B test_qualify.py` exits 0 with the offline PASS lines quoted (including a loopback test of the ws client against an in-process fake server on a temporary socket in the test's own tmpdir — that is not a Codex connection); RETURN six headings, §3 records R1–R6 for THIS carry per raw/baseline.md, §4 tests STATUS claims as paths; §6 uncertainty."
return_to: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/03.cartan.return.md
---

# POINT 03 — own the socket instead of finding it

## 0. Resolve your seat

`cartan`, thread ending `c57d8`. Mismatch → stop, RETURN §4, other fields `none`.

## 1. Why the route changed (head observation, plain shell, read-only — VERDICT 02 §Judgment)

There is no shared app-server daemon in use on this host: the working TUIs are attached to
nothing; the only listening Codex socket is a hand-started `codex app-server --listen
unix:///run/user/1000/codex-pocket-test.sock` with zero clients and unknown purpose. So the
disposable target will get **its own** server, and the socket path becomes the binding:

    [HANDS, sitting 1]   codex app-server --listen unix:///run/user/1000/nablarva-b-entry-20260913-01.sock
    [HANDS, sitting 1]   codex --remote unix:///run/user/1000/nablarva-b-entry-20260913-01.sock -C /tmp/nablarva-b-entry-20260913-01
    [engineering]        qualify.py inspect --sock /run/user/1000/nablarva-b-entry-20260913-01.sock --thread <observed uuid> --allow-connect <token>

Nothing in this cycle runs those. You design and document them.

## 2. `raw/route.r2.md`

- From official docs and installed help: what a TUI started with `--remote unix://PATH` is —
  is its conversation owned by that server (visible to `thread/list`, readable by
  `thread/read`, addressable by `queue --remote unix://PATH --thread <uuid>`)? Cite; mark
  unknown where the docs are silent. State the auth/usage-path expectation (same `~/.codex`
  auth → subscriber) and how the sitting will *observe* it (`/status` in the attached TUI).
- The exact three commands above, plus their read-only checks: server up (`ss -xl` line for
  the private path — the head runs that), `initialize` response, `thread/list` (expect the one
  thread), `thread/read` (status, activeFlags, ids). Exit codes and labels the reader emits.
- What replaces the plan's 0c "existing control socket" wording; what stays (0d admission
  contract still comes from characterization on THIS private server, never from the working
  sessions).
- The `codex queue --remote` doc/help mismatch (route r1 §3): what the sitting will test to
  settle it, later, under `armed_approval: send-once`.
- Hardening (Janus): override flags and profile-injected overrides on the *target* command
  line are BLOCKED by the validator; the reader inspects the target's effective settings via
  `thread/read`/`/status` fields where available and stops on any override signal.

## 3. `raw/probe/qualify.py` (revision)

- `inspect`: real transport, stdlib only — Unix socket, HTTP Upgrade handshake, RFC 6455 text
  frames, one JSONL message per frame, timeouts, byte bounds, no retries; `initialize` →
  `initialized` → `thread/list` → `thread/read`; prints only the allowlisted fields; refuses
  the two working thread ids; refuses to connect unless the `--sock` path exists as a socket
  AND `--allow-connect <token>` is given (token format defined in the file header; this cycle
  never mints one). Fail closed on every uncertainty.
- `validate`: add override-rejection of a supplied target command line / profile.
- No `send` verb still. Header lists every exit code and label.

## 4. `raw/probe/test_qualify.py` (revision)

Keep the six offline checks; add: ws handshake/frame codec round-trip against an in-process
fake server on a temporary Unix socket **inside a tmpdir the test creates and removes** (this
is a loopback of your own code, not a Codex connection — say so in the output label);
`inspect` without `--allow-connect` → BLOCKED with zero connect calls; override rejection
cases; a fake `thread/list`/`thread/read` response parse. All labels stay `UNVERIFIED-native`.

## 5. Baseline — this carry is the measured one

majkee carries this POINT with the PAD `pad.1-baseline-carry.md` and its voice listener; you
record R1–R6 (told · read · clarify · work · return · tell) with `date -Iseconds` timestamps in
RETURN §3, per `raw/baseline.md`. If you pull the POINT from the bed before any paste, say so in R1.

## 6. RETURN shape

Six headings; `none` explicit. §2 every command + outcome (test run verbatim; `codex queue
--help` and any other help verbatim if re-run). §3 hashes + R1–R6. §4 STATUS claims as paths;
record any cross-frame observation as attributed, not refuted. §5 one recommendation: the
exact contents of sitting 1's [HANDS] steps. §6 uncertainty. Bind to `cycle: "03"`, `from:
cartan`, `corrects: "02"`, this POINT. If `return_to` exists, stop. Tell majkee the path.
