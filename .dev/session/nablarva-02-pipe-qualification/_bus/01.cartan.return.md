---
cycle: "01"
from: cartan
to: oraculum
date: 2026-09-13
point: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/01.oraculum.point.md
point_sha256: daf69ae2cd8834a2b2d2c30aa977ee7121cecb01128c51b615e4109981792919
return_to: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/01.cartan.return.md
seat: "cartan · 01a092ed-f422-7051-ba69-f6c1d47c57d8 · hruzam-120922 · pane %50"
result: "P0 preflight and conditional qualification plan returned; no native send or qualification claimed"
preflight_sha256: fbebce0a9c245b539a4ff05fc63d31e2c72a1c50d8ab7093bd220647e2b68004
plan_sha256: 020f26808d3c41abc82423a5ffb5191ae23e2a24026d62d553ac027801c71ed9
---

# RETURN 01 — native evidence and the first qualification plan

## 1. Files changed

Created only the three POINT-authorized artifacts:

- [raw/preflight.r1.md](../raw/preflight.r1.md) — eight required sections; local observations,
  dated official documentation, inference and unknowns separated.
- [raw/qualification-plan.r1.md](../raw/qualification-plan.r1.md) — nine required sections;
  first sitting is read-only route/identity qualification, with conditional positive and
  negative cases, hands, counts and stop branches.
- This RETURN — exact cycle/POINT/seat binding and the six mandatory headings.

`return_to` was absent before creation. Both raw revisions are now handed off as immutable.
No probe/PAD/scratch/hook/config was authored; no head-owned file, old receipt, browser,
runtime transcript, board or portable source was edited. No commit, push, prune or deploy.
The dirty worktree predated this cycle; its unrelated changes remain with their owners.
Concurrent head additions during final validation are recorded in §4; they are not Cartan writes.

## 2. Commands and outcomes

### Frame and installed interface — observed here, not a helper's claimed runtime

All checks below ran on **2026-09-13**, in
`/home/hruzam/unikuklatrix/nablarva`, with this session's restricted filesystem permissions.
The native help inventory was independently rerun at **21:46:15 CEST (19:46:15 UTC)**;
each of its nine commands individually exited **0**.

| Exact command | Observed outcome / limit |
|---|---|
| `pwd` | `/home/hruzam/unikuklatrix/nablarva` |
| `hostname` | `hruzam-120922` |
| `printf '%s\n' "$CODEX_THREAD_ID" "$TMUX_PANE"` | `01a092ed-f422-7051-ba69-f6c1d47c57d8` and `%50`; POINT seat matches |
| `git branch --show-current` / `git rev-parse HEAD` | `core` / `b99701f4b04735a16cb51257d070ba91b01a96b6` |
| `date -Iseconds` | Sample `2026-09-13T21:46:43+02:00`; real clock available to this seat |
| `command -v codex` | `/home/hruzam/.local/bin/codex` |
| `readlink -f /home/hruzam/.local/bin/codex` | `/home/hruzam/.codex/packages/standalone/releases/0.154.0-x86_64-unknown-linux-musl/bin/codex` |
| `file /home/hruzam/.codex/packages/standalone/releases/0.154.0-x86_64-unknown-linux-musl/bin/codex` | Static PIE, stripped x86-64 ELF; not a local readable implementation of the queue contract |
| `codex --version` | `codex-cli 0.154.0` |
| `codex --help` | `queue`, `agents`, `app-server`, `resume`, `fork`, `migrate-rollouts` advertised; Unix/WebSocket remote forms; no startup `-n` advertised |
| `codex agents --help` | Interactive shared-daemon browser; no `--json` advertised; not an inert session listing result |
| `codex app-server --help` | Experimental app-server, daemon/proxy/schema tooling, stdio/Unix/WebSocket listeners |
| `codex app-server daemon --help` | Lifecycle verbs and `version`; no `status` verb; no operational verb executed |
| `codex app-server proxy --help` | Proxy to running control socket; `--sock`; no connection made |
| `codex resume --help` | UUID/name argument, UUID precedence, optional prompt; no resume performed |
| `codex fork --help` | Conversation UUID argument; no fork performed |
| `codex login --help` | Status subcommand exists; login/status/auth access not performed |

All nine help/version invocations printed the read-only PATH-alias warning and still returned
their requested output. No attempt was made to fix that warning. **No `codex queue` invocation
at all**, including help. Queue option details in the preflight are explicitly historical
bed-01 evidence, not an unreported P0 command. No agents browser/listing, daemon operation,
RPC/remote connection, session launch, migration or schema generation occurred.

### Current official documentation — fetched, not live behavior

Opened and checked on **2026-09-13**, using official-page retrieval and targeted text searches:

| Opened page | Evidence used |
|---|---|
| [CLI reference](https://learn.chatgpt.com/docs/cli/reference) | Searches for `codex queue` / `codex agents` did not establish those command contracts |
| [App Server](https://learn.chatgpt.com/docs/app-server) | Identity, state, completion and saved-history clauses cited in the preflight; no RPC run |
| [Developer commands](https://learn.chatgpt.com/docs/developer-commands) | Native naming/status and authentication-mode observation surfaces; none exercised on a target |
| [Advanced Configuration](https://learn.chatgpt.com/docs/config-file/config-advanced#notifications) | Notification event and payload documented, not installed |
| [Configuration Reference](https://learn.chatgpt.com/docs/config-file/config-reference#configtoml) | Notification configuration scope checked |
| [Hooks](https://learn.chatgpt.com/docs/hooks#stop) | Project hook/trust and Stop/Interrupt semantics checked; no hook run |
| [Changelog](https://learn.chatgpt.com/docs/changelog) | Installed-version release date checked; no post-September-12 change established |

Two bounded researcher helpers supplied native-notification and rollout-metadata leads;
Cartan fetched the load-bearing pages and reran the relevant local checks before integrating.
A bounded challenger reviewed the scope and drafts. It found no blocking draft error; its
distinct-fixture warning was folded into the negative cases. These are Tier B inputs, **not**
independent acceptance of Cartan's RETURN. Oraculum's P1 audit remains required.

### Read-only file/coordination checks

`git status --short` before authoring:

```text
 M .dev/session/pulse.md
?? .dev/session/GLOSS.nablarva.md
?? .dev/session/nablarva-01-design/
?? .dev/session/nablarva-02-pipe-qualification/
?? .dev/session/runbook-tool-00/
?? _mail/
?? meshup/a-symmetry-lightest/consult.relay-seam.round2.symmetry.2026-09-13.md
?? meshup/nablarva-01-design/
```

Exact targeted checks (paths below are relative to the stated repository unless absolute):

```sh
ls -la .dev/session/nablarva-02-pipe-qualification .dev/session/nablarva-02-pipe-qualification/_bus .dev/session/nablarva-02-pipe-qualification/raw
git log -1 --format='%H %s' -- .dev/session/nablarva-01-design
git ls-files -- .dev/session/nablarva-01-design .dev/session/nablarva-02-pipe-qualification
git diff -- .dev/session/pulse.md
rg -n 'target:' .dev/session/nablarva-02-pipe-qualification/STATUS.md
stat -c '%n | %F | %y' .dev/session/nablarva-02-pipe-qualification/STATUS.md .dev/session/GLOSS.nablarva.md .dev/session/nablarva-01-design .dev/session/runbook-tool-00 _mail meshup/nablarva-01-design meshup/a-symmetry-lightest/consult.relay-seam.round2.symmetry.2026-09-13.md
rg --files --hidden /home/hruzam/unikuklatrix/nablarva/.codex
rg -n --glob '*.md' --glob '*.py' --glob '*.toml' --glob '*.json' 'agent-turn-complete|hook_event_name|turn/completed|hooks.json' /home/hruzam/ia-sync/codex
ls -ld /tmp/nablarva-b-entry-20260913-01
git diff --check
```

Before authoring, the new bed contained only RUNBOOK, STATUS, POINT and fold note; no RETURN,
PAD or probe. Git log/tracked-file queries for the task beds produced no entries. Pulse diff
is exactly the two router rows STATUS describes. `rg 'target:'` matched prose/self-reference
at STATUS lines 12, 16, 17, 21 and 24, **not an actual target declaration**. The listed peer
paths exist. STATUS mtime was `2026-09-13 04:25:41.946223896 +0200`.
The project `.codex` directory exists but its file inventory was empty (`rg` exit 1);
the portable-source search found only a README exclusion, not a reusable completion probe.
Proposed scratch path was absent (`ls` exit 2); it was not created. `git diff --check` exited 0;
that alone does not check the untracked new Markdown.

Metadata-only rollout check, with its **captured** result in preflight §8:

```sh
stat -c 'path=%n | type=%F | bytes=%s | mtime=%y | ctime=%z | inode=%i' /home/hruzam/.codex/sessions/2026/09/12/rollout-2026-09-12T02-04-23-01a092ed-f422-7051-ba69-f6c1d47c57d8.jsonl
readlink -f /home/hruzam/.codex/sessions/2026/09/12/rollout-2026-09-12T02-04-23-01a092ed-f422-7051-ba69-f6c1d47c57d8.jsonl
```

No transcript contents or record types read. A later stat may differ as this live session
continues; that is not a delivery result or permission to edit its history.

Source reads included the project chain, POINT's required references, current STATUS and
fold/source consult, the pinned old design/VERDICT, and the relevant tunnel functions:

```sh
sed -n '150,190p' /home/hruzam/ia-sync/zsh/ai/tunnel-codex.py
sed -n '320,420p' /home/hruzam/ia-sync/zsh/ai/tunnel-codex.py
```

No tunnel command executed. The source findings in preflight §5 were confirmed directly,
not inferred from the guide or a helper's report.

Validation: an inline `python3 -B` check (PyYAML **6.0.3**) passed exact 8/9 section order,
frontmatter/POINT pin, local Markdown targets, balanced fences, terminal newlines and no
trailing whitespace. Its first draft incorrectly compared an unresolved `../` path with the
canonical pending RETURN path and exited 1; normalizing the validator's paths fixed the
check (exit 0), with no document change for that issue. The two final raw hashes are in the
frontmatter. The final package is also subject to Oraculum's rerun; no live qualification
test, timing, operator count or savings measurement is represented by these format checks.

The full-package check then caught **a concurrent STATUS change**, rather than a raw-artifact
failure: all three output structure/link checks passed, but the old STATUS pin no longer
matched. At **22:00:34 CEST**, the seven other protected inputs still matched; the owner had
added a second fold and a peer brief. Cartan read the changed STATUS and these exact new paths:

```sh
sed -n '1,240p' .dev/session/nablarva-02-pipe-qualification/raw/fold.relay-seam-v2.2026-09-13.md
sed -n '1,280p' meshup/a-symmetry-lightest/brief.relay-seam.v2.2026-09-13.md
```

The final draft's evidence boundary is therefore the original input plus the explicitly
recorded delta below, not a claim that shared state stood still. No other writer's changes
were reverted. For the new hidden-internal-turn claim, official-domain searches for
`Codex notify "hidden" "agent-turn-complete"` and `Codex "0.153" "notify" internal turns`
returned no results. The already fetched notification/hooks pages establish payloads, not
that specific observed behavior. **No installed hidden-turn notify test was run**: P0 forbids
hook installation, target launch and native sends. This is an explicit unverified check,
not a claim that the vendor never emits such events.

## 3. Evidence paths

Current bed:
`/home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/`.
Paths in this table are bed-relative except where explicitly parent-relative. These input
pins were checked before drafting and again after the two raw documents were finished:

| Input | SHA-256 |
|---|---|
| `RUNBOOK.md` | `c1248e0d21f0b8e18c648ef7763e0a751bb924f919ac1ede9d42ace4c337fcdf` |
| `STATUS.md` | `4ef95085fa7b4a408b9cbf54f4cccd5fbb75ff4bbad1b518b55f9ce213150f9d` |
| `_bus/01.oraculum.point.md` | `daf69ae2cd8834a2b2d2c30aa977ee7121cecb01128c51b615e4109981792919` |
| `raw/fold.relay-seam-round2.2026-09-13.md` | `614b4376e5c2ce1336fec6639cd593548be725b2e3df12b9ab009c0095704d00` |
| `../nablarva-01-design/raw/design.r1.md` | `b43d10ae13535e053547523044fb6c22e6448cc00f503457a60e48da3ab6814a` |
| `../nablarva-01-design/_bus/01.oraculum.verdict.md` | `df0af29b88fd5fbaf7767a2081095e59ace0b0cc33e2799c7baabc75845b501f` |
| `../pulse.md` | `423095be96c2d40bb8e68764670874516bce854a81ac372d69b5031c081b6d1d` |
| `../GLOSS.nablarva.md` | `337b1a58df946dd4585e0abd8fdf21873cd5a9df14381ac66866742c27441d3a` |

`sha256sum` was run with those eight exact repository paths. External source pin:
`/home/hruzam/ia-sync/zsh/ai/tunnel-codex.py` →
`75226a2c10eb932bb3716c8f2f804250b38f201969b2aacb64cbe6b8cbfef163`.

Other checked local evidence:

- [Current RUNBOOK](../RUNBOOK.md), [STATUS](../STATUS.md), [POINT](01.oraculum.point.md),
  [fold note](../raw/fold.relay-seam-round2.2026-09-13.md).
- [External seam consult](/home/hruzam/unikuklatrix/nablarva/meshup/a-symmetry-lightest/consult.relay-seam.round2.symmetry.2026-09-13.md)
  — historical/peer proposal, not a gavel; the source is repository-root `meshup/`.
- [Old STATUS](../../nablarva-01-design/STATUS.md),
  [preservation manifest](../../nablarva-01-design/raw/promotion-manifest.md),
  [old VERDICT](../../nablarva-01-design/_bus/01.oraculum.verdict.md) — current on-disk authority
  for closed/unpreserved, remaining count/commit authority and the B-entry burden.
- [BUS: Verifying a RETURN](/home/hruzam/reposoma/raw.guides/bus/GUIDE.md:118),
  [PAD: The PAD → log breathing](/home/hruzam/reposoma/raw.guides/PAD/GUIDE.md:93),
  [STATUS: The seam test](/home/hruzam/reposoma/raw.guides/status/GUIDE.md:26),
  [tunnel guide](/home/hruzam/reposoma/raw.guides/tunnel/GUIDE.md),
  [HANDSHAKE: Delivery rule](/home/hruzam/ia-sync/HANDSHAKE.md).

The raw artifacts and this RETURN contain the P0 evidence; there is no hidden probe output
directory to retrieve. The quoted native outputs can be rerun with the safe help/version
commands above. Live behaviors cannot be reconstructed from these files because none was
tested. The substrate read/ownership discipline followed Codex harness, official OpenAI
documentation and shared guide skills; no deployable primitive was created.

## 4. Mismatches discovered

### Every STATUS field/hold checked against its path or stated evidence boundary

This table checks the initial STATUS pin `4ef95085…50f9d`; its concurrent replacement and
every changed claim are checked in the following delta. Unchanged claims retain these findings.

| STATUS claim | Path/check and disposition |
|---|---|
| `updated` / `writer` | STATUS mtime observed in §2; no exact clock invented for Oraculum. RUNBOOK names Oraculum sole owner. These are an attributed snapshot and ownership, not a liveness claim. |
| `host` | Today's `hostname` confirms office. Historical `17:20Z` disagrees with old VERDICT `instruments` / Curvature 7: **17:20:33+02:00**, i.e. 15:20:33Z. Head may correct its timestamp attribution. |
| `worktree` | Branch/HEAD and every named dirty path match §2. Pulse really has two added rows. “Not re-verified today” was stale by this RETURN; no commit or unrelated edit made. |
| `gate` / branch B-entry | Gate agrees byte-for-text with RUNBOOK and pulse router row. B-entry is recorded in both task beds; user carried the current POINT. Historical public-transcript locator was not reread in P0 and is not promoted to fresh runtime proof. |
| `checkpoint`: opened, POINT amended, no outputs/target | The four initial files existed, no RETURN/PAD/probe. Current outputs supersede the old snapshot. No target was declared in the owner state; this is not a global proof that no disposable process exists anywhere. |
| Amendment justified by zero rollout mentions | **Not proof of non-consumption.** A filename-count result cannot establish what another live seat has or has not read. This RETURN binds the actual amended bytes consumed after the user's relay; the historical amendment premise remains unverified. Do not rewrite the immutable POINT to erase it. |
| `in_flight: none` | No native delivery was initiated in P0. This field/file inventory cannot establish worker activity: Cartan has now acted and returned. Head reconciles the snapshot; no transport attempt is inferred from it. |
| Recovery (a), only POINT/fold | That was the before-state; “carry may or may not have happened” was appropriately uncertain. Now user relay and this bound RETURN are actual evidence of work, not path-count inference. |
| Recovery (b), raw present ⇒ Cartan mid-work | **Overclaim.** Partial artifacts can also be interrupted/abandoned. Inspect available evidence; never infer an active worker or blindly re-dispatch from presence. |
| Recovery (c), RETURN ⇒ VERDICT then PAD | RETURN is ready for audit, not automatic advance. Branch on VERDICT **content/disposition**; REVISE/STOP does not authorize PAD execution. |
| Recovery (d)/(e), `target:` lookup/arming | `rg` showed self-matches in prose; there is no target tuple. Even a real tuple would not prove explicit arming. Require content-aware target/approval/attempt checks, exact instance and PAD report fences. |
| P0/no-send hold; amended POINT handed off | Scope respected by this seat. The current user relay is the observed handoff; future corrections need a new cycle. Claims about other sessions' earlier private activity remain outside this check. |
| Working-target exclusions | Cartan's full UUID verified from own environment; Oraculum's full ID read from STATUS. No connection/probe to either. Target deny fixtures are proposed, not a claim of live protection already built. |
| Native-first / no L4 fallback | RUNBOOK and flag hold; fold §1 correctly identifies the external actuator-order disagreement. External consult §1 is not permission for tmux. No native route was silently substituted. |
| Busy/approval/unknown; human nudges | No-submit rule preserved. “Human-fired” does **not** make a resend safe; see the load-bearing correction below. No busy queue experiment or nudge run. |
| Prohibitions: overrides, credentials, headless, watchers, cross-host, transcript edits | Applied to P0 and predeclared tests. No such action by Cartan; no prohibited probes were used to certify global absence of them. |
| Public pickup separate from metadata `observe()` | Implemented as an evidence boundary in the preflight; actual P0 rollout access was stat/readlink only. Filename/session ID/mtime do not certify a live endpoint. |
| Browser untouched; no Trajectory dependency | RUNBOOK excludes browser construction; no browser or runbook-tool-00 write/contact. Existing runbook-tool stub path present, not treated as current build authority. |
| Bed 01 closed/unpreserved; count and commit/prune owed | Old STATUS/manifest explicitly record these holds; scoped git queries show no preservation entry/tracked task files. Count not supplied here; no inferred preservation or authority. |
| No git/deploy/board/auth/cleanup; `.hlm` sealed | No authored change outside the three allowed files; no forbidden operation or `.hlm` read. Existing dirty paths preserved. |
| Single writer / helper allocation | Cartan wrote its two raw documents and RETURN; head surfaces unchanged by pins. Helpers were read-only and had no BUS/STATUS authority. Draft token-economy/cross-vendor chapters were not promoted to law. |
| `next` / `expected` | Carry-next is now stale: user carried it, Cartan read the exact amended POINT and returned. The three outputs exist; VERDICT/PAD/target still require the owner's next actions and gates, not file-presence inference. |

### Load-bearing document/interface curvature

1. **Consumption versus availability.** Fold §3 and POINT's positive-case wording identify
   consumption with `test -f` at turn end; owning RUNBOOK §Shared rules and r1 explicitly
   separate them. The plan preserves the stronger contract: hint → exact correlation/work
   check → independent acceptance. Oraculum must retain that distinction in its PAD/GLOSS.
2. **Nudge versus replay.** Fold §3 says completion without a receipt clears r1's no-blind-retry
   concern. It does not establish absence of effects or late publication. The plan permits
   no resend from that condition, even human-fired. Any retry needs reconciliation and new
   explicit authority; unknown stays stopped. This is the principal audit correction.
3. **Metadata versus native identity.** The external consult's metadata-based endpoint,
   reincarnation and liveness claims are stronger than what P0 can establish. The preflight
   limits metadata to evidence location/invalidation; instance/state qualification is separate.
4. **Installed spellings versus proposed conveniences.** `codex agents --json` and startup
   `-n` are not advertised. We did not execute them, infer a list schema, or borrow Claude's
   similarly named options. The target name is a label, never delivery authority.
5. **Local notification scope.** A project-local notify configuration is not supported by the
   fetched reference. Project Stop is a distinct candidate requiring actual qualification and
   explicit trust/config scope; no global workaround or automatic continuation is smuggled in.
6. **Stored-thread precedent versus living TUI.** Tunnel source has fresh-server/override and
   insufficient text-event filtering differences; reuse is reconciliation only. Its past PASS
   does not qualify this mechanism. CLI presence likewise does not qualify route/admission.
7. **Disposable receiver versus controller seat.** The RUNBOOK has only the two BUS labels.
   Plan uses an explicitly declared scratch test fixture, never a second `cartan` consuming
   this POINT or writing this RETURN. Head must name fixture authority before the sitting.
8. **No convenient weakening to force a pass.** P0 cannot establish native instance/admission
   fencing or acknowledgement semantics under its no-connection/no-queue limit. The executable
   first branch is read-only inspection; later send steps stay conditional, not falsely ready.

These are proposed audit dispositions, not a Cartan VERDICT or edits to Oraculum's files.

### Final-check delta — head update arrived during RETURN preparation

Observed **2026-09-13 22:00:34 CEST**, after the two raw revisions were finished:

| New/changed path | Observed SHA-256 |
|---|---|
| `STATUS.md` (mtime `2026-09-13 22:00:01.185121807 +0200`) | `a20a90ab5df266b08bda19f61ce7292f826d8d33abaf4b73c6b83db326ef5297` |
| `raw/fold.relay-seam-v2.2026-09-13.md` | `2de73f07b5266f6b0f798d8285415ef22cb10e40784c78c082055009187e8a2c` |
| Repository-root `meshup/a-symmetry-lightest/brief.relay-seam.v2.2026-09-13.md` | `9d7e1e2923889f275e83c5ce6641779d1801e967b9a7327c8e6c57081095c485` |

| Changed STATUS claim | Path/content check and disposition |
|---|---|
| Worktree/checkpoint/recovery (a): two head folds and frozen v2 brief | Both new files exist and were read in full. Their authority fields make them substrate, not Nablarva law. New brief appears in git's untracked list; it and the fold are peer-authored additions. “No RETURN” is stale at final check. |
| POINT already handed off; further changes in cycle 02 | POINT and RUNBOOK hashes are unchanged. Correct boundary: neither raw revision was rewritten to absorb late requirements. The head-requested two folds have now both been read. |
| Recovery (c): check hidden-internal-turn notify and metadata fields, otherwise cycle 02 | Metadata-only fields are covered in preflight §8. The specific hidden-turn behavior remains **UNVERIFIED**, with the safe checks/limits recorded in RETURN §2. The existing presence⇒progress flaw still needs disposition-aware branching. |
| Native-first actuator, not protocol | Confirmed as the fold's interpretation; no new transport authority, no L4 fallback. Queue remains a candidate actuator under the file contract, not the protocol itself. |
| No new `receipt` kind; “exchange complete” = RETURN present, not accepted | No receipt file created. Vocabulary mapping can denote availability only; it must not imply successful consumption/work/closure before the exact correlated RETURN and VERDICT checks. Existing RUNBOOK/r1 distinctions continue to bind. |
| Notify wakes a check; conditional auto-nudge deferred | Agrees with the drafts' attention-only handling. Neither a quiet window nor human firing reconciles a possibly executed assignment. No nudge is authorized by the new note. |
| Native binding ID, never a filename; `observe()` metadata-only | Compatible with the preflight: filename ID is only an evidence locator, never the binding. Codex notify's documented field is **`thread-id`**, not the fold's prose `thread_id`; hook/session-tree identifiers also require explicit mapping. |
| `next` carry and `expected` outputs | Actual amended POINT was consumed and this RETURN exists. Owner must refresh its next/checkpoint from these facts; no second carriage or automatic PAD/send follows. |

The v2 assertion about **0.153.x hidden internal turns** is attributed substrate, not a fresh
0.154.0 test result. Until qualified, treat any notify/Stop event as potentially insufficient
for exchange-level completion and require exact target/turn/fixture correlation plus the
artifact audit already in the plan. It cannot authorize native writes or a global notify
installation in this cycle. The new note's future `nablarva-03` is prospective, not an opened
build gate. The two raw r1 hashes remain unchanged; further technical work needs a new POINT.

## 5. Recommended next task

**Oraculum: perform P1 audit of this exact RETURN and the two pinned revisions**, including
the recovery, consumption and nudge corrections above. On ACCEPT, compose a **read-only first
PAD sitting** for opening/identifying the one disposable target and qualifying its existing
native route. Commission any required scoped probe through a later POINT before that sitting.
Do not offer the send step until route, incarnation, admission, acknowledgement and usage-path
preconditions are established and majkee has armed the exact target/action. A missing contract
is a STOP result to record, not permission for another actuator.

## 6. Remaining uncertainty

- Exact queue route, missing-daemon/unloaded-thread behavior, acknowledgement schema and
  native idle/approval/incarnation fence: **unverified**.
- Disposable target UUID, instance, permissions, live native UI and subscriber usage: **none
  observed**; no target opened or armed in P0.
- Installed Stop firing/trust/reload and signal correlation/delivery guarantees: **unverified**;
  no hook installed. The late v2 hidden-internal-turn claim was not reproduced on 0.154.0.
  Mtime is not a qualifying fallback.
- Resume/fork/rollover and direct persisted-edit effects: no live experiment; filesystem
  guarantees not established. Transcripts remain read-only evidence.
- All positive/live-negative/active-turn tests, completion/RETURN timing and operator counts:
  **NOT RUN**. No savings or generalized reliability claim.
- Historical amendment non-consumption premise, bed-01 sender count and preservation authority:
  not resolved by this preflight. Independent P1 acceptance remains Oraculum's work.
