---
cycle: "03"
from: cartan
to: oraculum
corrects: "02"
point: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/03.oraculum.point.md
point_sha256: 2585464c53444a466b26994968db72fc77ee5cc944fe1e484b122d97b5a5bcc5
status: "Engineering RETURN for head audit; UNVERIFIED-native; no opening, arming or send authority"
host: hruzam-120922
thread: 01a092ed-f422-7051-ba69-f6c1d47c57d8
pane: "%50"
worktree: "/home/hruzam/unikuklatrix/nablarva · core · 5be07a1125ae93725f122ad13515fae2f208b3a7 · dirty"
---

# RETURN 03 — bounded private-server reader

## 1. Files changed

Engineering complete for audit; native disposition remains STOP. The reader now implements
the direct Unix WebSocket route instead of the cycle-02 unwired stub. It cannot send and
does not emit QUALIFIED_READ_ROUTE. Nine offline/fake-loopback checks pass; these are not
native qualification.

Paths below are relative to
/home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/ unless absolute.

| Path | Change and owner |
|---|---|
| raw/route.r2.md | New private endpoint, exact future commands and gates, official evidence, uncertainty and sitting-1 proposal; Cartan root |
| raw/probe/qualify.py | In-place revision: canonical launch/profile gate, connection interlock, bounded stdlib Unix WebSocket initialize/list/read, metadata-only STOP output; bounded builder, Cartan integration |
| raw/probe/test_qualify.py | In-place revision: six existing categories retained/adapted, launch refusals, test-owned fake Unix round-trip, codec/limit/refusal checks; same builder |
| _bus/03.cartan.return.md | This six-section RETURN; Cartan root |

No other durable task file changed by this cycle. Fixture placement and schema did not
change, so raw/probe/fixtures/README.md and qualification.point.draft.md remain untouched.
STATUS, RUNBOOK, pulse, GLOSS, PAD, baseline, operator instruments and historical artifacts
remain their owners' surfaces. The existing reserved scratch remains empty, mode 700,
owned by hruzam (uid 1000).

The explicit POINT §4 exception allowed the tests to create and remove their own
/tmp/nablarva-qualify-test-*/fake.sock and enclosing temporary directory. No user data was
removed, no fixture was placed in the reserved scratch, and no Codex endpoint was contacted.
No valid live interlock was minted or supplied. No server/TUI/daemon/queue activation,
hook/config change, credential/history read, commit, push, deploy or cleanup.

Applicable skills: codex-harness kept protocol proof separate from authority/identity;
openai-docs routed current product claims to official sources; reposoma-surgical-coding
required before/after checks and preserved the narrow write scope. Guide consultation:
[BUS guide, RETURN and one-writer rules](/home/hruzam/reposoma/raw.guides/bus/GUIDE.md),
[STATUS guide, evidence not worker-liveness](/home/hruzam/reposoma/raw.guides/status/GUIDE.md),
[PAD guide, expected branch/report before action](/home/hruzam/reposoma/raw.guides/PAD/GUIDE.md).
No global harness primitive was created or promoted.

## 2. Commands and outcomes

Commands ran in the repository unless the test directory is specified. Long inspection
paths in the table are abbreviated relative to this bed. Repeated read-only context reads
are grouped; no runtime discovery command is implied by a path check.

| Command / inspection | Outcome |
|---|---|
| pwd; hostname; git status --short --branch; git log -1 --format=%H%n%s | Resolved this repository, host hruzam-120922, core, HEAD 5be07a1125ae93725f122ad13515fae2f208b3a7; existing dirty work preserved |
| printf '%s\n' "$CODEX_THREAD_ID" "$TMUX_PANE" | Exact thread ending c57d8, pane %50; matches POINT seat cartan |
| date -Iseconds (event samples; §3) | Europe/Prague +02:00 samples; no reconstructed sender timestamps |
| Applicable AGENTS chain presence/read; sed reads of flag, pulse, PROJECT.yaml, RUNBOOK, STATUS, POINT 03, VERDICT/RETURN 02, route/plan r1, baseline and PAD; selected guide/skill reads | Local authority and write boundaries resolved; no nested AGENTS found on the task path; sealed .hlm not read |
| sed/tail/rg reads of the two probe files, fixture README, route.r2; rg of old cycle references and RUNBOOK ownership | Static contract, codec/refusal paths, one writer and historical-reference discrepancies checked |
| ls -ld of route.r2.md and _bus/03.cartan.return.md before creation | Both absent (exit 2); no prior RETURN overwritten |
| rg -n '^  - (open_approval\|armed_target\|armed_approval):' STATUS.md (extended-regex alternatives in actual command) | Exit 1, no actual declaration lines; quoted templates do not arm anything |
| stat -c '%F %a %U %u %n' /tmp/nablarva-b-entry-20260913-01 | directory 700 hruzam 1000; exit 0 |
| ls -la -- /tmp/nablarva-b-entry-20260913-01 | Only . and ..; empty, exit 0 |
| ls -ld of STATUS's local artifact paths, including stenograph files, token-economy drafts, human-relay-time-logs, timeline, phone render, older beds/mail/meshup and communication-protocole | Present; paths only for operator artifacts, not acceptance or canon. pad.2-sitting-1.md separately absent |
| sha256sum -- on the §3 file sets, before/after comparison | Input pins preserved; authorized source revisions and new route pinned below |
| git diff --check | Exit 0; tracked-diff check only, not proof about the untracked task sources |
| rg --files --hidden raw/probe with __pycache__/pyc/pyo filters | Exit 1, no cache/bytecode paths returned |
| python3 -B - (inline, read-only stdlib artifact audit: AST, exact headings/pins, Markdown fences, non-verbatim whitespace, scratch/cache/temp and literal help checks) | Exit 0; all six artifact PASS lines below |

Artifact-check corrections: orchestration syntax errors dispatched no shell command.
The first dispatched audit exited 1 after the pin/AST/shape checks: its fence detector
misread an indented literal triple-backtick example inside verbatim CLI help. Corrected
the checker to respect code-fence indentation/closing syntax; no help bytes were changed.
The rerun exited 0:

```text
PASS artifact-pins 13 preserved inputs + 3 deliverables
PASS python-ast 2 sources
PASS return-shape 6 headings + cycle binding + R1-R6
PASS markdown-fences + non-verbatim whitespace
PASS scratch-empty-owner-mode + no test/cache residue
PASS installed-help outputs verbatim
```

No ss, pgrep, process/socket discovery, daemon verb, native inspection or native queue command
was run. The future commands in route.r2 are documentary, not this execution ledger.
Only codex queue --help was invoked. Official browsing was read-only: OpenAI App Server,
developer commands and authentication references, plus the POINT-requested generic RFC 6455.
No downloaded vendor source or generated schema.

Before-state runner (cycle-02 bytes), exit 0:

```sh
python3 -B /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/probe/test_qualify.py
```

```text
PASS offline_mock validator-refusals UNVERIFIED-native
PASS offline_mock admission-fence UNVERIFIED-native
PASS offline_mock read-unwired UNVERIFIED-native
PASS offline_mock cli-no-carrier UNVERIFIED-native
PASS offline_mock os-adapter UNVERIFIED-native
PASS offline_mock all-qualification-checks UNVERIFIED-native
```

Builder's first sandboxed revised-suite run reached its own temporary Unix socket and
failed at listener.bind(owned_sock): PermissionError: [Errno 1] Operation not permitted.
This is a test-local sandbox boundary, not a native Codex failure. The root immediately
requested scoped approval for the exact runner's fake socket; approval was granted.
The approved rerun exited 0 (completion sampled 2026-09-14T14:23:15+02:00). The approval
does not authorize any Codex endpoint, token, opening or send.

Final root runner after review fixes (absolute command below), exit 0; completion sampled
2026-09-14T14:27:41+02:00. Verbatim output:

```sh
python3 -B /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/probe/test_qualify.py
```

```text
PASS offline_mock validator-refusals UNVERIFIED-native
PASS offline_mock admission-fence UNVERIFIED-native
PASS offline_mock read-gated UNVERIFIED-native
PASS offline_mock cli-no-carrier UNVERIFIED-native
PASS offline_mock launch-evidence-refusals UNVERIFIED-native
PASS fake_unix_loopback ws-list-read UNVERIFIED-native
PASS offline_mock ws-refusals-bounds UNVERIFIED-native
PASS offline_mock os-adapter UNVERIFIED-native
PASS offline_mock all-qualification-checks UNVERIFIED-native
```

The fake server uses its own independent masking/frame decoder and HTTP accept response;
the client is the production lower-level reader. No valid interlock is supplied to the CLI
to exercise the positive fake case. CLI missing/invalid/non-ASCII token checks patch the
socket constructor and assert zero calls; launch/profile/working-ID refusals are independent.

Review corrections integrated before these final hashes: untrusted duplicate-key errors
no longer echo keys; malformed/deep JSON and non-ASCII tokens fail generically; peer-close
closes the descriptor; fake accept/read/EOF handling is bounded; the third working-TUI
suffix is excluded; effective_settings and native_incarnation remain explicitly unknown.
The route's future queue command was corrected to include installed-help-required --message.

Bounded helper review: architect p3_route_contract (read-only); builder p3_reader_build
(only the two source files); independent verifier p1_offline_gate (read-only fresh cycle-03
task). Helpers were not new BUS seats. The fresh verifier reported PASS for this engineering scope, independently reran all nine checks, and matched source/test hashes. It reviewed route hash 1a60bdc398a42af82d9e4eb318363c6f167013ebf60997cf1ba0655dfccea022; root then made only the wording clarification that a valid token is not materialized for invocation or supplied, and that the fake case calls the lower-level transport. The final route hash is pinned in §3. Cartan checked cited bytes/output before
integration; Oraculum still owns the acceptance VERDICT.

Fresh installed CLI evidence follows verbatim, including warnings. Each command exited 0.

### codex --version

```text
WARNING: proceeding, even though we could not create PATH aliases: Read-only file system (os error 30)
codex-cli 0.154.0
```

### codex --help

```text
WARNING: proceeding, even though we could not create PATH aliases: Read-only file system (os error 30)
Codex CLI

If no subcommand is specified, options will be forwarded to the interactive CLI.

Usage: codex [OPTIONS] [PROMPT]
       codex [OPTIONS] <COMMAND> [ARGS]

Commands:
  agents            Browse all agent sessions on the shared local app-server daemon
  exec              Run Codex non-interactively [aliases: e]
  review            Run a code review non-interactively
  login             Manage login
  logout            Remove stored authentication credentials
  mcp               Manage external MCP servers for Codex
  plugin            Manage Codex plugins
  app-server        [experimental] Run the app server or related tooling
  remote-control    [experimental] Manage the app-server daemon with remote control enabled
  completion        Generate shell completion scripts
  update            Update Codex to the latest version
  doctor            Diagnose local Codex installation, config, auth, and runtime health
  sandbox           Run commands within a Codex-provided sandbox
  debug             Debugging tools
  apply             Apply the latest diff produced by Codex agent as a `git apply` to your local
                    working tree [aliases: a]
  resume            Resume a previous interactive session (picker by default; use --last to continue
                    the most recent)
  queue             Queue a message for an existing session
  archive           Archive a saved session by id or session name
  delete            Permanently delete a saved session by id or session name
  migrate-rollouts  Inspect or migrate legacy local sessions to paginated thread history
  unarchive         Unarchive a saved session by id or session name
  fork              Fork a previous interactive session (picker by default; use --last to fork the
                    most recent)
  cloud             [EXPERIMENTAL] Browse tasks from Codex Cloud and apply changes locally
  exec-server       [EXPERIMENTAL] Run the standalone exec-server service
  features          Inspect feature flags
  help              Print this message or the help of the given subcommand(s)

Arguments:
  [PROMPT]
          Optional user prompt to start the session

Options:
  -c, --config <key=value>
          Override a configuration value that would otherwise be loaded from `~/.codex/config.toml`.
          Use a dotted path (`foo.bar.baz`) to override nested values. The `value` portion is parsed
          as TOML. If it fails to parse as TOML, the raw string is used as a literal.
          
          Examples: - `-c model="o3"` - `-c 'sandbox_permissions=["disk-full-read-access"]'` - `-c
          shell_environment_policy.inherit=all`

      --enable <FEATURE>
          Enable a feature (repeatable). Equivalent to `-c features.<name>=true`

      --disable <FEATURE>
          Disable a feature (repeatable). Equivalent to `-c features.<name>=false`

      --remote <ADDR>
          Connect the TUI to a remote app server endpoint.
          
          Accepted forms: `ws://host:port`, `wss://host:port`, `unix://`, or `unix://PATH`.

      --remote-auth-token-env <ENV_VAR>
          Name of the environment variable containing the bearer token to send to a remote app
          server websocket

      --strict-config
          Error out when config.toml contains fields that are not recognized by this version of
          Codex

  -i, --image <FILE>...
          Optional image(s) to attach to the initial prompt

  -m, --model <MODEL>
          Model the agent should use

      --oss
          Use open-source provider

      --local-provider <OSS_PROVIDER>
          Specify which local provider to use (lmstudio or ollama). If not specified with --oss,
          will use config default or show selection

  -p, --profile <CONFIG_PROFILE_V2>
          Layer $CODEX_HOME/<name>.config.toml on top of the base user config

  -s, --sandbox <SANDBOX_MODE>
          Select the sandbox policy to use when executing model-generated shell commands
          
          [possible values: read-only, workspace-write, danger-full-access]

      --approve-for-me
          Route approval requests through automatic review using the workspace-write sandbox

      --dangerously-bypass-approvals-and-sandbox
          Skip all confirmation prompts and execute commands without sandboxing. EXTREMELY
          DANGEROUS. Intended solely for running in environments that are externally sandboxed

      --dangerously-bypass-hook-trust
          Run enabled hooks without requiring persisted hook trust for this invocation. DANGEROUS.
          Intended only for automation that already vets hook sources

  -C, --cd <DIR>
          Tell the agent to use the specified directory as its working root

      --worktree
          Run the session in a new managed Git worktree

      --add-dir <DIR>
          Additional directories that should be writable alongside the primary workspace

  -a, --ask-for-approval <APPROVAL_POLICY>
          Configure when the model requires human approval before executing a command

          Possible values:
          - on-request: The model decides when to ask the user for approval
          - never:      Never ask for user approval Execution failures are immediately returned to
            the model

      --search
          Enable live web search. When enabled, the native Responses `web_search` tool is available
          to the model (no per‑call approval)

      --no-alt-screen
          Disable alternate screen mode
          
          Runs the TUI in inline mode, preserving terminal scrollback history.

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version
```

### codex app-server --help

```text
WARNING: proceeding, even though we could not create PATH aliases: Read-only file system (os error 30)
[experimental] Run the app server or related tooling

Usage: codex app-server [OPTIONS] [COMMAND]

Commands:
  daemon                Manage the local app-server daemon
  proxy                 Proxy stdio bytes to the running app-server control socket
  generate-ts           [experimental] Generate TypeScript bindings for the app server protocol
  generate-json-schema  [experimental] Generate JSON Schema for the app server protocol
  help                  Print this message or the help of the given subcommand(s)

Options:
  -c, --config <key=value>
          Override a configuration value that would otherwise be loaded from `~/.codex/config.toml`.
          Use a dotted path (`foo.bar.baz`) to override nested values. The `value` portion is parsed
          as TOML. If it fails to parse as TOML, the raw string is used as a literal.
          
          Examples: - `-c model="o3"` - `-c 'sandbox_permissions=["disk-full-read-access"]'` - `-c
          shell_environment_policy.inherit=all`

      --enable <FEATURE>
          Enable a feature (repeatable). Equivalent to `-c features.<name>=true`

      --disable <FEATURE>
          Disable a feature (repeatable). Equivalent to `-c features.<name>=false`

      --code-mode-host <URL>
          Connect to a remote code-mode host instead of starting a local host

      --strict-config
          Error out when config.toml contains fields that are not recognized by this version of
          Codex

      --listen <URL>
          Transport endpoint URL. Supported values: `stdio://` (default), `unix://`, `unix://PATH`,
          `ws://IP:PORT`, `off`
          
          [default: stdio://]

      --stdio
          Use stdio as the transport (equivalent to `--listen stdio://`)

      --analytics-default-enabled
          Controls whether analytics are enabled by default.
          
          Analytics are disabled by default for app-server. Users have to explicitly opt in via the
          `analytics` section in the config.toml file.
          
          However, for first-party use cases like the VSCode IDE extension, we default analytics to
          be enabled by default by setting this flag. Users can still opt out by setting this in
          their config.toml:
          
          ```toml [analytics] enabled = false ```
          
          See https://developers.openai.com/codex/config-advanced/#metrics for more details.

      --ws-auth <MODE>
          Websocket auth mode for non-loopback listeners
          
          [possible values: capability-token, signed-bearer-token]

      --ws-token-file <PATH>
          Absolute path to the capability-token file

      --ws-token-sha256 <HEX>
          Hex-encoded SHA-256 digest of the capability token

      --ws-shared-secret-file <PATH>
          Absolute path to the shared secret file for signed JWT bearer tokens

      --ws-issuer <ISSUER>
          Expected issuer for signed JWT bearer tokens

      --ws-audience <AUDIENCE>
          Expected audience for signed JWT bearer tokens

      --ws-max-clock-skew-seconds <SECONDS>
          Maximum clock skew when validating signed JWT bearer tokens

  -h, --help
          Print help (see a summary with '-h')
```

### codex queue --help

```text
WARNING: proceeding, even though we could not create PATH aliases: Read-only file system (os error 30)
Queue a message for an existing session

Usage: codex queue [OPTIONS] --thread <THREAD> --message <TEXT>

Options:
      --thread <THREAD>
          Session UUID or exact session name

      --enable <FEATURE>
          Enable a feature (repeatable). Equivalent to `-c features.<name>=true`

      --message <TEXT>
          Message text to queue

      --disable <FEATURE>
          Disable a feature (repeatable). Equivalent to `-c features.<name>=false`

      --remote <ADDR>
          Connect the TUI to a remote app server endpoint.
          
          Accepted forms: `ws://host:port`, `wss://host:port`, `unix://`, or `unix://PATH`.

      --remote-auth-token-env <ENV_VAR>
          Name of the environment variable containing the bearer token to send to a remote app
          server websocket

  -i, --image <FILE>...
          Optional image(s) to attach to the initial prompt

  -m, --model <MODEL>
          Model the agent should use

      --oss
          Use open-source provider

      --local-provider <OSS_PROVIDER>
          Specify which local provider to use (lmstudio or ollama). If not specified with --oss,
          will use config default or show selection

  -p, --profile <CONFIG_PROFILE_V2>
          Layer $CODEX_HOME/<name>.config.toml on top of the base user config

  -s, --sandbox <SANDBOX_MODE>
          Select the sandbox policy to use when executing model-generated shell commands
          
          [possible values: read-only, workspace-write, danger-full-access]

      --approve-for-me
          Route approval requests through automatic review using the workspace-write sandbox

      --dangerously-bypass-approvals-and-sandbox
          Skip all confirmation prompts and execute commands without sandboxing. EXTREMELY
          DANGEROUS. Intended solely for running in environments that are externally sandboxed

      --dangerously-bypass-hook-trust
          Run enabled hooks without requiring persisted hook trust for this invocation. DANGEROUS.
          Intended only for automation that already vets hook sources

  -C, --cd <DIR>
          Tell the agent to use the specified directory as its working root

      --worktree
          Run the session in a new managed Git worktree

      --add-dir <DIR>
          Additional directories that should be writable alongside the primary workspace

      --strict-config
          Error out when config.toml contains fields that are not recognized by this version of
          Codex

  -c, --config <key=value>
          Override a configuration value that would otherwise be loaded from `~/.codex/config.toml`.
          Use a dotted path (`foo.bar.baz`) to override nested values. The `value` portion is parsed
          as TOML. If it fails to parse as TOML, the raw string is used as a literal.
          
          Examples: - `-c model="o3"` - `-c 'sandbox_permissions=["disk-full-read-access"]'` - `-c
          shell_environment_policy.inherit=all`

  -h, --help
          Print help (see a summary with '-h')
```

## 3. Evidence paths

Final deliverable SHA-256 pins (this RETURN's self-hash is intentionally not embedded):

```text
eca59cc5783b5cffb7c4fb8f8806fa9848ee7b3eb6a3cce020cd2c8a19d05871  raw/route.r2.md
6c3c18c69b54f25681a627e26463a4a1409b3c5f771f0651dcb2af5fa1b09cf3  raw/probe/qualify.py
9071c2a16cbb472a4d526b1d6298f751919279cbd2ad73c49f691d9284669eb3  raw/probe/test_qualify.py
```

Before-state source pins, superseded only by this authorized revision:

```text
b5acf8eaa21557e2c0a550df16dc8b334a40106670909d4d9e5f83ae9357c1bb  raw/probe/qualify.py
b408b79af154d35761b5d6b1777fcc770b1dfc800641b9f3bb99ba11dd352b70  raw/probe/test_qualify.py
```

Starting/current preserved input pins, repository-relative paths:

```text
2585464c53444a466b26994968db72fc77ee5cc944fe1e484b122d97b5a5bcc5  .dev/session/nablarva-02-pipe-qualification/_bus/03.oraculum.point.md
a6b1828345c00b09ae9c1970c3506466f7682013f93076365b4af9af90fcd328  .dev/session/nablarva-02-pipe-qualification/STATUS.md
c1248e0d21f0b8e18c648ef7763e0a751bb924f919ac1ede9d42ace4c337fcdf  .dev/session/nablarva-02-pipe-qualification/RUNBOOK.md
edf0eec5265a9829116c7e0eba0d263fb8a35b0ddc23389d438fc185e7ae1885  .dev/session/nablarva-02-pipe-qualification/_bus/02.oraculum.verdict.md
7d766326924ef1d034aa201680f6ee11cfa3419920a8874d73aad006b2e1b918  .dev/session/nablarva-02-pipe-qualification/_bus/02.cartan.return.md
ac1c9c8e74647ffe87bc1f1ab0fc1366c36536da19b819fe15ca4d8eea3b4314  .dev/session/nablarva-02-pipe-qualification/raw/route.r1.md
020f26808d3c41abc82423a5ffb5191ae23e2a24026d62d553ac027801c71ed9  .dev/session/nablarva-02-pipe-qualification/raw/qualification-plan.r1.md
bce671f98c585cac4fbfe00ddc9ff79b603515c478aadf5f826f3811fab85129  .dev/session/nablarva-02-pipe-qualification/raw/probe/fixtures/README.md
a6cf1639958941e1c097ad9c824bf9005abf6ca3c48f2b56c679832729efa87f  .dev/session/nablarva-02-pipe-qualification/raw/probe/fixtures/qualification.point.draft.md
623589a68137a6e00f14ebef1584a4594187beda4c05588dee615932d09981de  .dev/session/nablarva-02-pipe-qualification/pad.1-baseline-carry.md
65a752cadf750ffc98d2ea857e4ec318eddbc8a62d5e670918c4808210c24a1e  .dev/session/nablarva-02-pipe-qualification/raw/baseline.md
423095be96c2d40bb8e68764670874516bce854a81ac372d69b5031c081b6d1d  .dev/session/pulse.md
842eb89cc7c54ac472bdaa005341c1d670260d32458b3bc74daff67efac93671  .dev/session/GLOSS.nablarva.md
```

Current official evidence is linked beside its claims in raw/route.r2.md §1:
[App Server](https://learn.chatgpt.com/docs/app-server),
[developer commands](https://learn.chatgpt.com/docs/developer-commands),
[authentication](https://learn.chatgpt.com/docs/auth),
[RFC 6455](https://www.rfc-editor.org/rfc/rfc6455.html).
Installed-help evidence is reproduced in §2, not substituted for live behavior.

Baseline remeasure — **bed 02, cycle 03, this carry**. All recorded shell clock samples
below use date -Iseconds on hruzam-120922 (+02:00). Event times not observed exactly are
labeled as samples/bounds; later repeated reads do not replace the first-read measurement.

| id | Event | Reading |
|---|---|---|
| R1 | TOLD | Operator supplied the exact POINT 03 carry in the current user message; not an own-clock pull. First clock sample after receipt/frame entry: 2026-09-14T14:04:15+02:00. Actual arrival instant and clipboard method/count were not separately observed. |
| R2 | READ | First read of the exact POINT from disk completed by the immediately following sample 2026-09-14T14:04:32+02:00. |
| R3 | CLARIFY | 0 human clarification questions before work. Separately, after work began, one scoped test-only permission prompt was required for the sandbox-blocked fake Unix bind; granted, with successful rerun completion sampled 2026-09-14T14:23:15+02:00. It is a permission interaction, not an inferred sender S-count. |
| R4 | WORK | Start sample 2026-09-14T14:04:32+02:00 (after first POINT read); end/seal sample 2026-09-14T14:39:27+02:00. This is model/tool work including review and the permission wait, not human labor. |
| R5 | RETURN | First file creation bracketed by two date samples, both 2026-09-14T14:34:39+02:00; final seal clock sample 2026-09-14T14:39:27+02:00. Final artifact recheck precedes the notice; no self-hash is embedded. |
| R6 | TELL | User-facing RETURN-ready notice via this session's tool output, followed by the final copyable @oraculum handoff. Clock sample before sealing/checking/notifying: 2026-09-14T14:39:27+02:00. This is the notice's clock sample, not an observed UI arrival or human-read time. |

Sender S1–S9, REAL flush, lifecycle marks and human waiting/action counts are **unknown**
to Cartan until supplied. No savings claim. R6 records a notice emitted by this session,
not proof that majkee read it; sender S8/receipt remains separate. Oraculum transcribes
the actual readings into its existing baseline/timeline surfaces.

## 4. Mismatches discovered

STATUS source is the pinned STATUS.md dated 2026-09-14T12:35:49+02:00; these are checks of
its fields and paths, not a replacement STATUS or a claim about another seat's runtime.

| STATUS field / hold | Path/evidence check and limit |
|---|---|
| updated; writer | STATUS.md exists and matches its starting pin. Writer declares oraculum/claude; Cartan did not edit it. File authorship is not a process-liveness signal. |
| host | Own hostname agrees with the declared office host. |
| worktree | Repository/core agree; actual HEAD is 5be07a1125ae93725f122ad13515fae2f208b3a7, not STATUS's b99701f4b04735a16cb51257d070ba91b01a96b6. Own log subject: stenograph skill: rewrite in first-person (ICH form) voice. Dirty listing also includes .dev/session/nablarva-communication-protocole/ and now partly tracked bed paths. No provenance guessed or unrelated commit changed. |
| gate; decision_rule | Both remain in STATUS.md and agree with RUNBOOK and plan r1: native ack, submission-boundary busy/approval rejection, incarnation fence, worked RETURN, subscriber usage and UI evidence still owed. Offline PASS does not satisfy GO. |
| checkpoint | _bus/02.oraculum.verdict.md exists, REVISE closes cycle 02; POINT 03 exists. New route/reader/RETURN now need head audit. No current native launch/inspection evidence was produced. |
| in_flight | "none" is the head's declaration at its timestamp, not proof of absence of processes. Bounded native helpers on this cycle were not new BUS participants. |
| recovery_probe (a–c) | Before work, route.r2 and RETURN 03 were absent. During build the route/source changes were partial evidence. At publication RETURN 03 exists, so branch (c) calls for audit, not automatic acceptance, liveness or duplicate POINT. |
| recovery_probe (d–f) | The exact approval-declaration search returns no lines. Nothing is armed. ss count was NOT measured here: a missing approval cannot prove the external count is zero. Head must make that read-only check later; quoted templates are not approval records. |
| holds: three-stage ladder | open_approval → observed armed_target → separately scoped armed_approval remains mandatory. New route §2 uses it. Native UUID and required tuple must precede reader arming; if UI cannot supply them, no tokenless discovery. |
| holds: baseline | raw/baseline.md and pad.1-baseline-carry.md exist and are unchanged. This is the actual operator-carried POINT 03, not pull. R1–R6 are in §3; sender REAL flush remains majkee/Oraculum's evidence, not inferred. Old cycle references are listed below. |
| holds: pocket/working sessions | STATUS now attributes pocket-test to majkee's earlier experiment; POINT/VERDICT 02 still say unknown purpose. Historical observation is not refuted. No seat connected to or stopped it. Two full working IDs and the known third suffix are refused by the probe. |
| holds: engineering-only / interlock | No native contact/start/token. POINT's blanket no-socket/no-outside-write wording has a specific exception in done_when and §4 for a test-created temporary fake Unix server; this cycle used only that exception. The sandbox separately required scoped permission to bind it. |
| holds: override rejection | qualify.py validates exact server and target argv plus required null profile-absence claim; any extra token or present/missing profile is BLOCKED. Null/clean argv is not evidence of effective settings; unknown stays STOP. |
| holds: consumption, nudge, native-first, L4, BUS kinds | No send verb, nudge, terminal input injection, hook, retry carrier or new BUS kind. Successful read/file presence is neither task receipt nor completion. |
| holds: fixture identity | Existing draft/README unchanged; only intended receiver write stays /tmp/nablarva-b-entry-20260913-01/qualification.return.md. Scratch remains empty; probe_codex is not a real seat. |
| holds: operator artifacts | Both res/skills/stenograph files, raw/brief_nablarva-token-economy_2026-09-14.md, raw/fold.token-economy.2026-09-14.md and raw/human-relay-time-logs/ exist. Presence only checked; no adoption as gavel or editing of the listener instrument. |
| holds: head discipline / changed surfaces note | GLOSS, PAD, baseline, timeline and raw/view/pad.1-baseline-carry.html paths exist; current head note is on disk. Pinned GLOSS/PAD/baseline unchanged during this cycle. Timeline/render contents were not used as authority; phone-render presence is not acceptance. |
| holds: transcript pickup | No transcript/rollout pickup this cycle, no histories/auth/config read. observe-style output remains allowlisted metadata. |
| holds: browser and old bed | .dev/session/runbook-tool-00/ and nablarva-01-design/ exist and remain untouched. Bed-01 count/commit-ok remain owed there; this task neither proves them nor opens a browser extension branch. |
| holds: git/sealed scope/single writer | Existing dirty work preserved; .hlm not read; no commit/push/prune/deploy/board/auth/cleanup. Only four authorized durable files written. Source revisions pinned; no head writes to Cartan artifacts. |
| holds: token-economy / allocation | Draft paths present; bounded helpers follow RUNBOOK §Shared rules and majkee's allocation, not a new seat economy or architectural lock. |
| next | POINT 03 has now been carried and acted on. Its pocket-origin question is already answered in the current holds. Head should advance this after its audit; Cartan did not rewrite it. |
| expected | New route, revised code/tests, six-section RETURN and R1–R6 are supplied; scratch is empty. Sender REAL flush, Oraculum transcription/audit and pad.2-sitting-1.md remain separate head/operator steps. pad.2 was absent when checked. |

Specific curvature requiring attribution, not silent flattening:

1. raw/baseline.md correctly names cycle 03 at its top/readings, but its recipient heading
   still says RETURN 02 and R1 says POINT 02. pad.1-baseline-carry.md STEP 0's branch says
   RETURN 02, STEP 1's pull branch says POINT 02, and STEP 4 pairs with RETURN 02 §3.
   Those stale branches could misclassify this carry. POINT 03 §5 and the latest user
   instruction govern this RETURN 03 measurement. Repair belongs to Oraculum, not Cartan.
2. POINT's "JSONL per frame" is tightened to one JSON-RPC object per WebSocket text message.
   Its "expect the one thread" is an acceptance condition under exact cwd/state-DB-only
   filtering, not a promise that a private endpoint has an isolated persisted database.
   route.r2 §1/§4 cites the official transport/list semantics.
3. Same Codex home does not establish subscriber usage. The server can inherit ChatGPT
   or API-key login; inspect does not call account/read or config/read. Null profile
   evidence is only a supplied absence claim, not proof of effective model/permissions.
4. Head-observed lack of a shared daemon in VERDICT 02 is dated plain-shell evidence,
   not a fresh root runtime survey. Cycle 02 forbade root socket/process discovery:
   its absence of a survey was a scope limit, not a demonstrated technical impossibility.
   This cycle did demonstrate a different sandbox restriction: the fake bind EPERM.
   Fresh CLI help retains the PATH-alias warning; the head's earlier empty stderr and
   .codex ENOENT remain cross-frame observations, not refuted or repaired here.
5. The VERDICT 02 prose says all sixteen pins match while its verification table records
   15/16 plus the head's later GLOSS edit. Preserve the explicit dated exception; current
   cycle pins start after it and match. No reconstruction of the older state was needed.
6. VERDICT 02's uncertainty table suggests a proxy fallback; POINT 03 selects the direct
   stdlib WebSocket reader. No automatic proxy/alternate-carrier fallback was implemented.
7. Fixture README retains cycle-02 unwired-reader wording because POINT allows its edit
   only on changed fixture placement, which did not occur. route.r2 explicitly supersedes
   the reader/testing description without rewriting that historical pin.

## 5. Recommended next task

One recommendation: **Oraculum audits RETURN 03, then on ACCEPT authors
pad.2-sitting-1.md from route.r2 §2 and requests open_approval only.** No permission for
those steps is inferred from this RETURN.

Exact contents to release sequentially as the later sitting's HANDS steps:

1. Reconcile approvals/prior work; verify the existing scratch is empty, real, owned and
   mode 700. Head checks the exact new private socket path is absent and not listening.
   A pre-existing path stops setup; no unlink/reuse. No touch of pocket-test.
2. Ask majkee for one sitting's open_approval for the named private server plus one TUI,
   exact workspace/default settings and expiry. No UUID is invented before opening.
3. With actual server-terminal cwd verified as the scratch, start the exact command in
   the first block below; the head checks the exact private address with the listed ss
   filter and full-field inspection. This observes a listener, not native incarnation.
4. In a second scratch terminal, open the one TUI with the exact second command. Majkee
   uses native /rename then /status; report UUID/name/cwd/host/opened_at, remote address,
   version, available effective settings, usage-path observation and current instance
   evidence. Stop on identity, trust/auth, override or usage ambiguity; do not repair.
5. Record only a complete observed armed_target. If UUID cannot be obtained before the
   first read, stop for a separately approved source. No tokenless list/name discovery.
6. Ask separately for armed_approval: inspect, bind observed launch JSON and the public-data
   interlock, then release exactly route.r2 §4's reader command once. Report its actual
   metadata/STOP label in the PAD. Unknown settings/incarnation remain unknown. End this
   sitting before any send; missing evidence requires a scoped next proposal.

Future commands only, NOT RUN:

```sh
codex app-server --listen unix:///run/user/1000/nablarva-b-entry-20260913-01.sock

codex --remote unix:///run/user/1000/nablarva-b-entry-20260913-01.sock -C /tmp/nablarva-b-entry-20260913-01

ss -xl | rg -F -- '/run/user/1000/nablarva-b-entry-20260913-01.sock'
```

The later remote-queue mismatch test is not a seventh opening/read step. It requires an
audited, explicitly armed send-once/characterization branch with qualified admission and
instance requirements, or a separately approved experiment to characterize them.
No change to STATUS, PAD or GLOSS is bundled into Cartan's lane.

## 6. Remaining uncertainty

No native server/TUI was opened or observed by this cycle; private listener existence is
unknown here. No actual disposable UUID, native incarnation fence, effective settings,
subscriber usage, noninterference, queue acceptance, native acknowledgement, boundary-time
busy/approval guard, recovery contract or correlated worked RETURN has been established.
The fake test establishes only this codec/read implementation against a synthetic peer.

The client deliberately refuses fragmentation/binary messages, extra list results,
pagination and unexpected protocol shapes; real vendor compatibility can therefore
fail closed. Initialized/version metadata, socket ownership/inode and optional peer UID
checks do not authenticate an incarnation. The deterministic interlock is not authority
and has no expiry; STATUS/PAD and the operator provide action permission. Exact-argv
claims do not inspect aliases, environment or inherited configuration.

No native PASS, delivery/savings claim, canon lock or broader build authorization follows.
Sender-side action counts and actual human receipt times remain unknown until reported.
Oraculum's audit and majkee's action-specific gavel remain required.
