---
cycle: "01"
point: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/01.oraculum.point.md
return: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/01.cartan.return.md
audited_revisions: "raw/preflight.r1.md sha256 fbebce0a9c245b539a4ff05fc63d31e2c72a1c50d8ab7093bd220647e2b68004 · raw/qualification-plan.r1.md sha256 020f26808d3c41abc82423a5ffb5191ae23e2a24026d62d553ac027801c71ed9"
verified_by: "oraculum · 2026-09-13 · hruzam-120922 (office)"
disposition: ACCEPT
gate_effect: "advanced — P0 complete; phase P1 (probe code, then PAD sitting 1) opens; gate not closed; no target, arming, or send authorized by this VERDICT"
status_rewritten: yes
instruments: "own reads of the three artifacts and every cited local path; @assay read-only rerun (2026-09-13T22:05:55+02:00); @epoch dated verification of seven vendor claims; @janus adversarial pass on plan r1 and on the head's lean. This seat has no shell; command results are the rerun's."
---

# VERDICT 01 — P0 accepted; the plan's caution is correct and the head adopts it

## Judgment

The RETURN meets POINT 01's `done_when`: preflight (8 sections) and plan (9 sections) in the
required order, dated installed-interface observations behind every native claim, a six-field
RETURN whose §4 tests every STATUS claim as a path and adds a disclosed final-check delta for the
head's concurrent edits, and explicit `unknown`/`NOT RUN` wherever P0 could not look. Nothing
outside the three authorized files was written; the scratch path was checked absent and not created.

Three corrections in RETURN §4 land on the head and are accepted without argument: (1) two
*Claude* flags were projected onto Codex in the POINT (`codex agents --json`, startup `-n`) —
neither exists on 0.154.0; (2) the head's fold loosened "consumption" into `test -f` at turn end —
r1 and the RUNBOOK keep availability, consumption and acceptance distinct, and the plan restores
that; (3) the head over-cleared the nudge — a completed turn with no RETURN is *unreconciled*, not
"safe to nudge"; no resend, human-fired or not, without reconciliation and a separately approved
retry step. These are now STATUS holds.

Janus found no RUNBOOK/r1 violation in the plan, dissolved the head's "0d deadlock" worry (the
plan's own ordering resolves it: read route → approved characterization → 0d *from* that evidence →
final guard → send), and inverted the risk: the danger is **over-release**, not stall — a validator
refusal or an approved characterization quietly promoted to native proof. Its rule is adopted
verbatim in STATUS before any sitting: **default disposition STOP**; `UNVERIFIED-native` is never
PASS; GO only when native acknowledgement, busy-rejection and incarnation-fence are each observed
live on the disposable target.

## Claim verification

| RETURN claim | cited evidence | observed (rerun / docs / own read) | disposition |
|---|---|---|---|
| Only preflight, plan, RETURN written | bed listing | `ls -la`: exactly those + POINT + two head folds; no probe/, no PAD | CONFIRMED |
| Pins: POINT `daf69ae2…`, preflight `fbebce0a…`, plan `020f2680…`, RUNBOOK `c1248e0d…`, folds, design.r1, pulse, GLOSS, tunnel-codex.py | frontmatter, §3, §4 delta | all recomputed hashes MATCH; STATUS `4ef9…` stale as disclosed, new `a20a90ab…` MATCH; STATUS mtime `22:00:01.185121807` exact | CONFIRMED |
| Headings/cycle/from/point/pins validate | §2 inline validator | independent PyYAML 6.0.3 check: 8/8 PASS (link/fence/whitespace subset not rerun) | CONFIRMED |
| `codex-cli 0.154.0`, static musl binary at `…/releases/0.154.0-…/bin/codex` | §2 | `readlink -f` identical | CONFIRMED |
| `codex --help`: `queue · agents · app-server · resume · fork · migrate-rollouts`; **no `-n`/`--name`** | §2 | verbatim; no name option anywhere | CONFIRMED — corrects head's POINT §1.2 |
| `codex agents --help`: shared-daemon browser; **no `--json`** | §2 | verbatim | CONFIRMED — corrects head's POINT §1.1 |
| `app-server daemon --help`: no `status` verb; `proxy --help`: `--sock` to running control socket | §2 | verbatim | CONFIRMED |
| `resume --help` UUID-or-name (UUID precedence); `fork --help` UUID; `login --help` has `status` | §2 | verbatim; none executed | CONFIRMED |
| Project `.codex/config.toml` ignores `notify` → workspace-local notify not eligible | config-reference | quoted: "Codex ignores … `notify` … in a project-local `.codex/config.toml`" | CONFIRMED (load-bearing) |
| `notify` payload keys hyphenated: `thread-id`, `turn-id`, `last-assistant-message` | config-advanced | quoted list matches | CONFIRMED — corrects head's fold prose `thread_id` |
| Project `.codex/hooks.json` Stop: trust-gated; fields `session_id · cwd · hook_event_name · turn_id · stop_hook_active · last_assistant_message`; `block` → continuation; Interrupt separate; async out-of-order/cancelled | hooks#stop | all quoted | CONFIRMED |
| `thread/read` without loading; status `notLoaded · idle · systemError · active+activeFlags`; approval-waiting is an active flag; resume uses `thread.id`; fork → new id; `sessionId` = session-tree root; resume alone doesn't touch rollout mtime | app-server | all quoted; approval flag confirmed by example only | CONFIRMED (flag: M) |
| CLI 0.154.0 = Sept 9; nothing after 09-12 | changelog | quoted | CONFIRMED |
| `codex queue` route/daemon/no-daemon/turn-boundary semantics undocumented | preflight §Route "unknown" | official docs + app-server-daemon README: nothing; third-party prose only | CONFIRMED as NOT DOCUMENTED |
| Hidden-internal-turn Stop/notify firing unverified | §2, §4 delta | hooks page silent | CONFIRMED as NOT DOCUMENTED |
| `/status` reports model · approval policy · writable roots · usage | developer-commands | page truncated on two fetches; not quoted | UNVERIFIED (docs), to be observed live at HANDS 2 |
| tunnel-codex.py `:168` fresh stdio server · `:325` `approvalPolicy: never` · `:380` `item/completed` unfiltered | §2 sed | exact lines 171 · 326 · 380 | CONFIRMED |
| `/tmp/nablarva-b-entry-20260913-01` absent (exit 2) | §2 | reproduced | CONFIRMED |
| Project `.codex` dir "exists but empty" | §2 | `ls -la .codex` → **No such file or directory** | **REFUTED** (absent, not empty; `rg --files` cannot tell the two apart) |
| Nine help/version commands printed a PATH-alias warning | §2 | isolated stderr = 0 bytes on three commands; no alias | **REFUTED in rerun** (session-dependent or inaccurate; harmless) |
| `rg 'target:'` matches STATUS prose at 12,16,17,21,24 — no declaration | §2 | now 12,16,17,21,25 after the disclosed head edit; qualitative claim holds | CONFIRMED (line pin stale by one, explained) |
| Seat = `01a092ed…c57d8`, pane `%50` | `$CODEX_THREAD_ID` | seat-local; consistent with rollout filename and operator declaration | UNVERIFIED (attributed, consistent) |
| Rollout metadata (size/inode/mtime) | preflight §8 | live file, later mtime; metadata only | UNVERIFIED (expected drift) |

## Curvature

1. **Head projected Claude flags onto Codex** (POINT §1.1 `codex agents --json`, §1.2 `-n`). Neither
   exists. Cartan neither ran nor inferred them. Brand-blindness slip; the standing witness caught it.
2. **Head loosened consumption** (fold round-2 §3; POINT §2.3 wording). Corrected: `test -f` at turn
   end = availability/attention; consumption = native target/turn correlation + the fixture's fresh
   challenge worked; acceptance = this seat's VERDICT.
3. **Head over-cleared the nudge** (fold round-2 §3). Corrected: unreconciled ≠ safe to nudge. No
   resend without reconciliation proving the prior attempt cannot still act, plus an approved retry step.
4. **Head's amendment premise** ("zero rollout mentions ⇒ not consumed") was weaker than stated; the
   outcome is sound (Cartan consumed the amended bytes, pin `daf69ae2…`) and the POINT is not touched again.
5. **Head's timestamp attribution** in STATUS `host:` ("17:20Z") was wrong: the rerun clock was
   17:20:33+02:00 = 15:20:33Z. Retyped.
6. **Head's recovery probe** (b) "raw present ⇒ mid-work" and (c) "RETURN ⇒ VERDICT then PAD" overclaim;
   (d)/(e) `target:` grep self-matches prose. Retyped: branch on VERDICT disposition; arming is a
   distinct key `armed_target:` with an approval line, never inferred from a tuple.
7. **Notify key spelling** — fold prose `thread_id` vs documented `thread-id`. Noted; no file edit.
8. **Cartan's two refuted P0 statements** — `.codex` absent not empty; PATH-alias warning not
   reproduced. Neither bears on the plan; Cartan records both in its next RETURN §4.
9. **Concurrent head edits during Cartan's draft** (STATUS, fold v2, brief v2) — Cartan pinned both
   states and read the new files; correct handling, no reversion. The head will not edit bed surfaces
   while a POINT is open without noting it in STATUS first.
10. **Fixture identity** — `probe_codex` is a declared test fixture, not a seat; the head names its
    authority and write scope in the PAD before arming (Cartan curvature 7). Adopted.
11. **HANDS 1–2 need the scratch `mkdir` and P2 arming** (Janus) — cycle 02 authorizes the absence
    check + `mkdir -m 700`; STEP 0 hard-blocks HANDS 1 on "scratch present ∧ armed".

## Uncertainty disposition

| Uncertainty | Where it goes |
|---|---|
| Queue route / daemon / no-daemon / ack schema / turn-boundary semantics — undocumented | **Cycle 02**: Cartan documents the control-socket location and read path from official sources (no connection) and builds the read-only observer; **sitting 1** (0c) observes the route on the disposable target; **characterization** (majkee-approved) supplies 0d's evidence. |
| Native idle/approval/incarnation fence at the submission boundary | 0d from characterization evidence; if not demonstrable live → **STOP** (default pole). |
| Hidden-internal-turn Stop/notify firing | Optional later Stop probe on the scratch target, approved at arming; until then any turn-end signal is a hint only. |
| `/status` field list | Observed live at HANDS 2 by majkee; recorded in the PAD fence; not assumed. |
| Disposable target identity, permissions, usage path | None until P2 arming; recorded under `armed_target:` with a separate approval line. |
| Sender-side baseline (bed 01) | majkee; non-blocking; no savings claim in this bed either way. |
| Cartan's seat/pane attribution; rollout metadata drift | Accepted limits; corroborated, not proven. |
| Link/fence/whitespace checks of the raw files | Not independently rerun; low risk; accepted. |

## What this VERDICT authorizes and what it does not

Authorizes: cycle 02 (read-only probe code + offline fixtures + scratch absence check and `mkdir`),
then the head composes `pad.1-b-entry.md` from the audited plan with GLOSS notes before each step.
Does not authorize: opening, naming, arming, connecting to, or sending to any session; hook
installation; any override; any change to the working sessions. Arming is majkee's, per sitting.
