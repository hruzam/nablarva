---
cycle: "02"
point: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/02.oraculum.point.md
return: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/02.cartan.return.md
audited_artifacts: "raw/route.r1.md ac1c9c8e… · raw/probe/qualify.py b5acf8ea… · raw/probe/test_qualify.py b408b79a… · raw/probe/fixtures/qualification.point.draft.md a6cf1639… · raw/probe/fixtures/README.md bce671f9… · RETURN 02 7d766326…"
verified_by: "oraculum · 2026-09-14 · hruzam-120922 (office) — this seat now has a shell; own read-only host observations are marked as such"
disposition: REVISE
gate_effect: "unchanged — P1 stays open; cycle 02 closed as an honest PARTIAL; corrective cycle 03 opened in the same head decision; no target, opening, arming, connection or send authorized"
status_rewritten: yes
instruments: "own reads of RETURN 02, route.r1, fixtures, README; own read-only host observation (pgrep, ss -xlp/-xp, ls of ~/.codex and /run/user/1000, config key names only); @assay rerun + static transport audit + offline suite (2026-09-14T00:26:04+02:00); @janus adversarial pass on the head's corrective lean; @field flush of both beds (view only)"
---

# VERDICT 02 — honest PARTIAL; the missing fact was outside Cartan's sandbox

## Judgment

RETURN 02 delivers five of six `done_when` clauses and says plainly which one it does not:
`route.r1.md` could not name a concrete control-socket location or a releasable STEP 0c
command, so `inspect` is deliberately unwired (`STOP_ROUTE_UNKNOWN`). Everything else is
verified: all sixteen pins match; the offline suite reproduces byte-for-byte (six `PASS … UNVERIFIED-native` lines);
the static audit finds no reachable socket, subprocess or connect in either file and no
`send` verb at all; the scratch directory exists, mode 700, empty; frontmatter and headings
parse. Per `res/fanout-turns.md`, REVISE closes this cycle without satisfying the branch, and
the head opens one corrective cycle — POINT 03 — in the same decision.

The blocker Cartan hit is real and was **unresolvable from inside its sandbox**: it may not
list sockets or processes. From a plain shell the head observed (read-only, no connection):

- the only listening Codex Unix socket on this host is `/run/user/1000/codex-pocket-test.sock`,
  held by a hand-started `codex app-server --listen unix://…` (pid 645707, socket file 2026-09-12
  00:09, uptime 2 days) with **no external clients** — only its own internal socketpair;
- neither living TUI (`codex resume …c57d8` in tmux `nablarva-cartan:cartan-oStar`; the ia-sync
  `…9cea3c` one) listens on or connects to any daemon socket; their unix streams are socketpairs
  to their own `codex-code-mode-host`/sandbox children;
- `~/.codex/app-server-control/` holds only an empty `app-server-startup.lock`; no `*.sock` under `~/.codex`.

Consequence: on this host there is **no shared daemon that the working sessions use**, so the
"discover the shared socket" route is empty, and connecting to the pocket-test server would be
connecting to a server of unknown ownership. Janus reached the same conclusion from the law
side: a reader must never share a server with the working sessions. The route that satisfies
both is a **private app-server per disposable target** (`codex app-server --listen
unix://<private>` + the target TUI launched `--remote unix://<private>`): the socket path *is*
the endpoint binding, chosen by the operator at open time; `thread/read` and any later `queue
--remote` address that server only. This is the head's proposal for POINT 03; Cartan decides
the engineering and states what the docs do and do not establish about `--remote` TUIs.

## Claim verification (condensed; full rerun in the head's instruments)

| RETURN claim | observed | disposition |
|---|---|---|
| Six authorized files + empty scratch; nothing else written | listing + `find` for caches: exact | CONFIRMED |
| All §3 pins | 15/16 MATCH; `GLOSS.nablarva.md` MISMATCH — head appended it 00:22:37, after the report clock (Curvature 2) | CONFIRMED (mismatch is the head's) |
| Offline suite: six PASS lines, exit 0 | identical output | CONFIRMED |
| No transport reachable from `inspect`/`validate`; no `send` verb; no import-time effects | static audit: only `os.open` no-follow reads; argparse rejects `send` (exit 2); `dont_write_bytecode` | CONFIRMED |
| `.codex` "exists but empty (dr-xr-xr-x)" in Cartan's frame | plain shell: ENOENT (twice) | cross-frame, cause unknown — attributed, not refuted |
| PATH-alias warning in Cartan's frame | plain shell: empty stderr | cross-frame — attributed |
| R1–R6 recipient baseline for the POINT 02 exchange | present; unknowns explicit; R3 = 0 human clarifications | CONFIRMED; transcribed to raw/baseline.md |
| STATUS `next:` stale after this RETURN | true | CONFIRMED (Curvature 3) |

## Curvature

1. **Head's POINT 02 wording "two refuted P0 statements"** — over-stated. Assay's ENOENT/empty-stderr and Cartan's empty-dir/warning are observations from two frames (plain shell vs Codex sandbox projection); neither refutes the other. Recorded as cross-frame, cause unknown, no repair attempted.
2. **Head edited a surface while a POINT was open without a STATUS note first** — `GLOSS.nablarva.md` appended at 00:22:37 (PAD note) after Cartan's 00:02:56 report clock; Cartan's pin is therefore stale through no fault of its own. Own hold violated; named here; the note is now in STATUS.
3. **STATUS `next:` stale** — still said "carry POINT 02" after the RETURN existed. Replaced.
4. **Opening ≠ arming** (Cartan curvature 3; fixtures README §2) — `armed_target` needs a UUID that only exists after the TUI is opened. Adopted as a three-stage ladder in STATUS: `open_approval:` (majkee authorizes starting the private server and opening ONE TUI attached to it, default settings) → `armed_target:` (observed tuple) → `armed_approval:` (inspect | send-once | characterize). No placeholder tuple ever arms.
5. **Route premise corrected by host observation** — no shared daemon in use; pocket-test server is an orphan of unknown purpose (question to majkee); the design moves to a private server per target. Cartan's "do not guess a socket" was right; the answer is not to find one but to own one.
6. **Janus hardening** — the reader and any later carrier must *reject* override flags and profile/config-injected overrides (`--dangerously-*`, `--approve-for-me`, `-a never`, `--model`, `-p`, `-c` overrides on the target), not merely omit them; an isolated pair (private server) is required before any connection near a working session.
7. **Proxy framing unknown** — `codex app-server proxy --sock` byte convention undocumented; the Unix-socket transport is documented as WebSocket + HTTP Upgrade. Both routes to the *private* server are ours to test; neither is assumed.
8. **Sender count for the POINT 02 exchange was never measured** — the instrument (`raw/baseline.md`) was defined after that carry. Recorded `unknown`; the remeasure is the POINT 03 carry with `pad.1-baseline-carry.md` and its listener rules. R1–R6 for cycle 02 are transcribed.

## Uncertainty disposition

| Uncertainty | Where it goes |
|---|---|
| Concrete route for a disposable target; whether a `--remote` TUI's conversation is server-owned and visible to `thread/list`/`thread/read`; whether `queue --remote` honors the option | **POINT 03** (docs + design + reader, no launch) then **sitting 1** (private server + attached TUI, read-only) |
| Proxy vs direct WebSocket framing | POINT 03 implements one (direct ws over unix, stdlib) and specifies the other as fallback; sitting 1 observes |
| Non-interference with working sessions | structural: private server never hosts them; sitting 1 additionally records that both working TUIs remain untouched (operator observation) |
| Native ack / busy rejection / incarnation fence | later sittings, per plan r1; default STOP stands |
| Cross-frame `.codex`/PATH observations | accepted limit; no investigation |
| pocket-test server origin | majkee, one line; never connected to by any seat |
| Sender-side baseline | POINT 03 carry, with the PAD |

## What this VERDICT authorizes / does not

Authorizes cycle 03 as written in POINT 03: engineering and documentation only. Does not
authorize starting any server, opening any TUI, connecting to any socket, installing hooks,
or sending. Those begin only after `open_approval:` is transcribed into STATUS by the head
from majkee's explicit word, and only in the PAD sitting.
