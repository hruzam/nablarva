---
cycle: "02"
from: oraculum
to: cartan
scope: "P1 engineering, read-only side: the observer/validator probe in read mode, its offline refusal/race/correlation tests, the fixture drafts, the documented control-socket read route, and the scratch directory (absence check + mkdir). No send mode executed, no target, no connection."
paths:
  read:
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/01.oraculum.verdict.md   # curvature 1–11; decision rule
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/STATUS.md                     # decision_rule, armed_target/armed_approval shape
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/preflight.r1.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/qualification-plan.r1.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/raw/design.r1.md                          # §Uncertainty after submission, §Acceptance test
  write:
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/probe/qualify.py
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/probe/test_qualify.py
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/probe/fixtures/qualification.point.draft.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/probe/fixtures/README.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/route.r1.md
    - /tmp/nablarva-b-entry-20260913-01/                                                                          # mkdir -m 700 only, after absence check; stays empty in this cycle
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/02.cartan.return.md
gates: "No `codex queue` invocation except `codex queue --help` (record verbatim). No app-server daemon start, no proxy/socket connection, no `thread/read`, no session launch, no `codex agents` listing, no hook/notify install, no config change, no credential inspection, no send of any kind. qualify.py's send mode may exist as code but must be unreachable without an explicit `--armed <approval-token>` argument that this cycle never supplies; the test suite must prove read mode cannot submit. Tests are offline: mocks clearly labeled; no import-time connection. The only write outside the bed is the empty scratch directory. No STATUS/RUNBOOK/pulse/GLOSS/PAD write. Do not read /home/hruzam/unikuklatrix/nablarva/.hlm/. Corrections to r1 artifacts are new revisions, never edits."
done_when: "All six write paths exist; `python3 raw/probe/test_qualify.py` (or the declared runner) exits 0 with the offline PASS/FAIL lines quoted in RETURN §2; raw/route.r1.md names the documented control-socket location and the exact read-only command(s) STEP 0c would run, with source citations and every unknown marked; the scratch dir exists mode 700, owner hruzam, empty; RETURN six headings, §4 tests every STATUS claim as a path and records the two refuted P0 statements (`.codex` absent; PATH-alias warning not reproduced) as curvature; §6 uncertainty."
return_to: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/02.cartan.return.md
---

# POINT 02 — the read-only half of the probe, nothing that can send

## 0. Resolve your seat

`cartan`, thread ending `c57d8`. Mismatch → stop, RETURN §4, other fields `none`.

## 1. `raw/route.r1.md` — the documented read route (docs + help only)

From official sources and installed `--help` (no connection): where the shared local
app-server daemon's control socket lives for this user; how `codex app-server proxy --sock`
reaches it; the exact JSON-RPC read a STEP 0c observer would issue (`thread/read` with the
target's `thread.id`, summary only, no resume/subscribe); what fields it returns (status,
activeFlags, ids, updatedAt); what a *missing* daemon looks like; whether a read can disturb
either working session (expected: no; state your evidence). Also `codex queue --help` verbatim
on 0.154.0 and every statement the docs make about queue — mark "not documented" plainly.
Every unknown stays unknown; nothing is discovered by connecting.

## 2. `raw/probe/qualify.py` — observer/validator, read mode only

Foreground, stdlib-only Python. Modes:
- `inspect --sock <path> --thread <uuid>` — one bounded read; prints route/version, thread id,
  status, activeFlags, updatedAt; labels `QUALIFIED_READ_ROUTE` / `STOP_ROUTE_UNKNOWN` /
  `STOP_IDENTITY_UNKNOWN` / `STOP_NOT_IDLE`. Refuses the two working ids
  (`01a092ed-f422-7051-ba69-f6c1d47c57d8`, `0a27e884-b106-48e6-b56a-71dcb271ab6f`).
- `validate --fixture <path> --tuple <json>` — strict POINT-shaped fixture validation (frontmatter,
  `to: probe_codex`, exact `return_to` under the scratch, destination absent, hash), duplicate
  keys rejected, symlink escapes rejected, name-only targets rejected. Prints `BLOCKED` with reason
  or `PREPARED` with the frozen preview (hashes, tuple, generation).
- `send` — may exist but requires `--armed <token>` and an unused-attempt marker; **not exercised**
  this cycle; the tests prove it cannot run without both. No retry, no retarget, no fallback,
  no daemon management, no name search, no rollout parsing, no overrides, no credentials.
Fail closed on any uncertainty; exit codes documented in the file header.

## 3. `raw/probe/test_qualify.py` — offline fixtures with carrier-call counters

Cover at least: wrong UUID/bed/seat/destination → `BLOCKED`, zero carrier calls; exact-name
collision → never selects; stale tuple (generation mismatch) → rejected; duplicate request in one
controller → second call blocked, counter stays 1; after simulated restart → `UNRECONCILED`, not
"unused"; snapshot-to-submit state change (idle→active between preview and send) → blocked;
read mode has no path to the carrier (assert by inspection of call graph or by monkeypatched
counter). Label every mock; a mock pass is `UNVERIFIED-native` by name in the output.

## 4. `raw/probe/fixtures/` — drafts, not placed

`qualification.point.draft.md`: POINT-shaped (`from: oraculum`, `to: probe_codex`, scope = read this
fixture + one small synthetic input, compute a deterministic checksum/transform, write exactly one
RETURN at `/tmp/nablarva-b-entry-20260913-01/qualification.return.md`, identify fixture hash,
per-attempt challenge, observed conversation id, commands, destination; refuse on mismatch or
existing destination). Challenge left as `{{CHALLENGE}}`; the head materializes it per attempt in
the PAD. `README.md`: what the fixture is, what it never is (not BUS cycle traffic), how it is
placed at arming, by whom.

## 5. Scratch directory

`test ! -e /tmp/nablarva-b-entry-20260913-01 && test ! -L /tmp/nablarva-b-entry-20260913-01 &&
mkdir -m 700 -- /tmp/nablarva-b-entry-20260913-01`; record `ls -ld`. If it exists: stop, report,
do not reuse or delete. Nothing is written inside it this cycle.

## 6. RETURN shape

Six headings; `none` explicit. §2: every command with outcome, the test run verbatim. §3: evidence
paths + file hashes. §4: STATUS claims as paths (this cycle's STATUS carries `decision_rule:` and
the `armed_target:`/`armed_approval:` shape — say whether the probe's labels and the PAD can honor
them); record the two refuted P0 statements. §5: what sitting 1 should contain, one recommendation.
§6: uncertainty. Bind to `cycle: "02"`, `from: cartan`, this POINT. If `return_to` exists, stop.
Tell majkee the RETURN's absolute path.
