---
cycle: "03"
corrects: "02"
point: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/03.oraculum.point.md
return: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/03.cartan.return.md
audited_artifacts: "raw/route.r2.md eca59cc5… · raw/probe/qualify.py 6c3c18c6… · raw/probe/test_qualify.py 9071c2a1… · RETURN 03 5f17d4f9…"
verified_by: "oraculum · 2026-09-14 · hruzam-120922 (office)"
disposition: ACCEPT
gate_effect: "advanced — P1 engineering complete; sitting 1 may be composed and armed by the ladder; gate not closed; native qualification remains STOP by default; no opening, arming, connection or send authorized by this VERDICT"
status_rewritten: yes
instruments: "own read of RETURN 03, route.r2, fixtures; @assay rerun + static socket-scope audit + offline suite (2026-09-14T14:58:35+02:00); @janus adversarial pass on the head's sitting-1 lean; own read-only git/host checks"
---

# VERDICT 03 — the reader is real and cannot reach anything; my isolation claim was not

## Judgment

RETURN 03 meets every `done_when` clause. `route.r2.md` names the private-server route with the
exact server, attach and reader commands (FUTURE, not run), the later `queue --remote` candidate
(NOT RUN), the doc/help mismatch, and every unknown as unknown. `qualify.py inspect` now
implements the documented Unix WebSocket transport in stdlib (HTTP Upgrade, RFC 6455 frames,
one JSON-RPC object per text message, bounds: 5 s, 8 KiB header, 64 KiB frame, 256 KiB / 32
frames) behind three gates — exact private socket path, `--allow-connect` interlock, inline
`--launch-evidence` whose exact argv rejects every override form — and there is still no `send`
verb. The rerun confirms: one `connect()` site (qualify.py:644) reachable only through those
gates; the loopback test binds solely inside a tmpdir it creates and removes; nine PASS lines
byte-identical; no socket left behind; the pocket-test socket unchanged before and after. R1–R6
are logged with clock samples; this carry was the operator's paste, not a pull.

Janus's REVISE lands on the head: VERDICT 02 called non-interference "structural". Cartan's own
route §1 says the private server is *not* a separate user, configuration, auth store or thread
database — only the transport is private. Corrected below and converted into a checkable
bracket around the server launch in `pad.2-sitting-1.md` STEP 0/3 (metadata only: listing of
`~/.codex/app-server-control/`, mtime of `~/.codex/auth.json`, `ss -xl` exact-path count; any
change → the sitting stops). "Expect exactly 1" on the filtered `thread/list` remains the
namespace fence; 0 or 2 → STOP.

## Claim verification (condensed)

| RETURN claim | observed | disposition |
|---|---|---|
| Four durable files changed; scratch untouched; fixtures untouched | listing + pins | CONFIRMED |
| 3 deliverable + 13 input pins | 16/16 MATCH; 2 before-state pins correctly superseded | CONFIRMED |
| Reader connect gated: exact path · interlock · host · launch evidence; no send | code at :338–339, :644, :749–761; `inspect --sock /nonexistent.sock` → BLOCKED exit 2 before any socket | CONFIRMED |
| Loopback test tmpdir-only, cleaned in `finally` | test_qualify.py:425, :500–502 | CONFIRMED |
| Nine PASS, exit 0 | reproduced byte-for-byte; `ss` count 0 after; no `*.sock` residue | CONFIRMED |
| Sandbox EPERM on the fake bind, scoped approval granted | attributed (Cartan's frame) | ATTRIBUTED |
| Only pocket-test socket listening; no discovery run by Cartan | rerun `ss -xlp` identical | CONFIRMED |
| Fresh CLI help with PATH-alias warning in Cartan's frame | not re-run; cross-frame as before | UNVERIFIED (accepted) |
| R1–R6 with samples (R1 14:04:15 · R2 14:04:32 · R3 0 · R4 14:04:32→14:39:27 · R5 14:34:39 · R6 14:39:27) | RETURN §3 | CONFIRMED as recorded; transcribed to raw/baseline.md |
| Six headings; `corrects: "02"`; POINT pin | PyYAML check PASS | CONFIRMED |

## Curvature

1. **Head over-claimed isolation** (VERDICT 02 uncertainty row "structural"). Corrected: transport-private only; shared `~/.codex`. Non-interference is now a bracketed, falsifiable check in the PAD, not an assertion. Janus.
2. **STATUS `worktree:` stale** — HEAD is `5be07a1` (majkee's commit "stenograph skill: rewrite in first-person", 03:35), not `b99701f`; `res/skills/stenograph/skill.stenograph.md` is now tracked while the rest of the bed is not. Retyped.
3. **Three stale cycle-02 references** in `pad.1-baseline-carry.md` and one heading in `raw/baseline.md` survived the retarget (Cartan curvature 1). Fixed 14:58–15:05 before this VERDICT; named here.
4. **VERDICT 02 prose "sixteen pins match" vs its table 15/16** — sloppy sentence; the table was right. Preserved as written; noted.
5. **POINT 03's "JSONL per frame"** was loose; route.r2 tightens to one JSON-RPC object per WebSocket text message. Accepted.
6. **Interlock is intent, not authority** — the `--allow-connect` token is deterministic public data (sha256 of path+NUL+uuid). It prevents accidental connection; STATUS `armed_approval:` and the PAD provide permission. Accepted as designed; never described as a secret.
7. **Same Codex home ≠ subscriber usage** — login mode must be observed in the attached TUI's `/status`; unknown stays unknown. Accepted.
8. **New operator directory** `.dev/session/nablarva-communication-protocole/raw/LLM-esperanto.md` (BabelTele as an optional payload codec) — a fork, parked; not this bed; not law.

## Uncertainty disposition

| Uncertainty | Where it goes |
|---|---|
| Whether a `--remote` TUI's thread is server-owned and visible to the filtered `thread/list`; effective settings; subscriber login mode; native incarnation | **Sitting 1** — observed, never inferred; STOP on 0/2 results or any unknown that the ladder requires |
| Auth-store / daemon-namespace side effects of launching a private server | **Sitting 1 STEP 0/3 bracket** (metadata only); change → STOP |
| `queue --remote` support and semantics; acknowledgement; busy/approval rejection; incarnation fence | later sitting under `armed_approval: send-once` / `characterize`; default STOP stands |
| Sender-side S1–S9 for this carry | majkee's REAL flush; transcribed verbatim when given |
| Cross-frame PATH/`.codex` observations | accepted limit |

## What this VERDICT authorizes / does not

Authorizes the head to compose `pad.2-sitting-1.md` and to ask majkee for `open_approval`.
It does not start a server, open a TUI, mint an interlock, connect, or send. Those happen only
inside the sitting, one step at a time, after the STATUS ladder lines exist.
