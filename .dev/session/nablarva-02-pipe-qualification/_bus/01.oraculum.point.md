---
cycle: "01"
from: oraculum
to: cartan
scope: "P0 — read-only native preflight and an executable qualification plan for one native operator-triggered assignment into a disposable, already-running Codex session; operator-hands steps marked; no native send."
amended: "2026-09-13, before consumption (cartan rollout shows zero mentions of this path at amendment time) — added preflight §1.7 and §1.8 from the relay-seam round-2 consult; nothing else changed"
paths:
  read:
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/RUNBOOK.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/STATUS.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/fold.relay-seam-round2.2026-09-13.md  # head note: what the seam consult folds here, incl. the L4 ordering collision
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/raw/design.r1.md          # founding reference, pinned b43d10ae…
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/_bus/01.oraculum.verdict.md # §Curvature 9–11 name what B-entry must prove
    - /home/hruzam/reposoma/raw.guides/PAD/GUIDE.md                                                   # the shape your operator-hands steps will take
    - /home/hruzam/reposoma/raw.guides/tunnel/GUIDE.md                                                # prior stored-thread precedent; reconciliation principle only
    - /home/hruzam/ia-sync/zsh/ai/tunnel-codex.py                                                     # read-only reference; not to be transplanted
  write:
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/preflight.r1.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/raw/qualification-plan.r1.md
    - /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/01.cartan.return.md
gates: "READ-ONLY cycle. No `codex queue` invocation, no app-server daemon start or connection, no session launch, no `codex agents` listing that could disturb the working sessions unless it is documented read-only (then record exact command and output), no hook or notify install, no config change, no credential inspection, no send of any kind. No STATUS/RUNBOOK/pulse/GLOSS write. Do not read /home/hruzam/unikuklatrix/nablarva/.hlm/. Native in-window helpers allowed; their claims are yours to check. Both raw files are immutable once handed off — corrections are r2 in a new cycle."
done_when: "raw/preflight.r1.md and raw/qualification-plan.r1.md exist with the sections below; 01.cartan.return.md has the six mandatory headings; §2 carries dated installed-interface observations (exact commands + outcomes) for every native claim; §4 tests every STATUS claim as a path and names every doc-vs-disk mismatch; §6 states what remains unverified; nothing else was written."
return_to: /home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-02-pipe-qualification/_bus/01.cartan.return.md
---

# POINT 01 — preflight and plan, no send

## 0. Resolve your seat

You are `cartan`, the existing independent Codex session (thread id ending `c57d8`). Mismatch →
stop, RETURN §4 reports it, other fields `none`.

## 1. `raw/preflight.r1.md` — what the installed surface actually offers (observed, dated)

Sections, each `##`, observed facts first, inference marked:
1. **Route** — how `codex queue --thread <UUID|name> --message` reaches a *living interactive*
   session: shared local app-server daemon? socket path? `--remote`? What happens with no daemon.
   What `codex agents` / `codex agents --json` lists and whether listing is side-effect-free.
2. **Identity vs incarnation** — what identifies the conversation (thread UUID, exact name via
   `-n`/session name) and what identifies the running instance (pid, daemon registration,
   `$CODEX_THREAD_ID` in the target's own environment); what changes on `codex resume`,
   `fork`, restart; how a stale or replaced target would be detected *before* a send.
3. **State** — app-server `thread/read` `status` values and `activeFlags`; whether they are
   readable without loading/resuming; what `queue` does when the target is `active`,
   `waitingOnApproval`, `notLoaded` — as far as docs/help say; mark unknown as unknown.
4. **Permissions and usage path** — which sandbox/approval settings the disposable target will
   run with (defaults only; no overrides); how subscriber usage can be *observed* without
   credential inspection (e.g. the TUI's own usage display, `codex login status`); what would
   indicate an API-path turn instead.
5. **Read-back** — how the target's completed turn is read (app-server `thread/read`, rollout
   JSONL public items, the RETURN file itself); correlation to the exact POINT/`return_to:`.
6. **Vendor facts refreshed** — versions (`codex --version`), dated pages; anything shipped after
   2026-09-12 that changes the picture.
7. **Turn-complete signal** *(added 2026-09-13)* — does installed Codex expose a turn-complete
   hook or `notify` program (config key, payload fields: thread id, turn id, last message)?
   Can it be configured **workspace-locally** for a disposable target without touching global
   config? If absent or unstable: rank the fallbacks — app-server `turn/completed` /
   `thread/read` status, then rollout mtime. This is the Codex analogue of Claude's Stop hook
   and the basis of the "expected RETURN exists at turn end" check.
8. **Rollout semantics and observe() metadata** *(added 2026-09-13)* — is the rollout JSONL
   append-only during a live run and the read path only on `resume`/`fork`? On resume: same
   file, rollover, or a new file with duplicated items (the false "new session" trap)? Which
   **metadata-only** fields identify an endpoint and its reincarnation — existence · path ·
   session id · mtime · rollover — with no record types and no content read. State plainly
   whether editing a rollout could alter a resumed session's belief (expected: yes → never
   touch; transcripts are read-only evidence).

## 2. `raw/qualification-plan.r1.md` — the executable plan

1. **Target definition** — what majkee must open: `codex` in a scratch workspace
   (propose the exact path, e.g. `/tmp/nablarva-b-entry/` or a bed-local scratch), named
   session, default permissions; what the head records in STATUS `target:` (UUID · name ·
   workspace · host · opened_at).
2. **Preconditions / STEP 0** — state check that proves the target is living, idle, bound, and
   is *not* one of the working sessions; reversible.
3. **Positive case** — one POINT-shaped assignment delivered by `codex queue` (or the qualified
   route), the target writes its RETURN at the exact `return_to:`; observations recorded
   separately: prepared · submitted (ack?) · consumed · RETURN present · accepted. If §1.7
   yields a usable turn-complete signal, the consumption check is *expected RETURN exists at
   turn end*; its configuration on the disposable target is declared here and approved by
   majkee at arming, never installed silently.
4. **Negative cases**, each with predeclared required result: wrong/stale target (UUID of a
   closed session; exact-name collision) · replacement (same conversation resumed in a new
   instance) · duplicate attempt (second queue of the same POINT) · interruption after possible
   submission (kill the queue client / the TUI mid-flight) · busy / awaiting approval (r1 rule:
   no submission; what the plan does instead).
5. **Characterization (optional, labeled, needs majkee's explicit approval per sitting)** —
   queue while the target is mid-turn: is the message delivered at the next turn boundary,
   injected, or refused? Never a delivery path in this gate; observation only.
6. **Counts** — operator actions per category (setup · routine carriage · recovery · audit/gavel)
   with the same granularity r1 fixed; who counts; where the number lands (PAD report fence).
   Count nudges (a second activation with the same pointer after a completed turn with no
   receipt) separately; nudges are human-fired in this bed.
7. **Operator-hands steps** — marked `[HANDS]`, each copy-pasteable with absolute paths and an
   expected output; everything else is autonomous engineering (yours) or head navigation.
8. **Stop conditions** — unintended activation/retarget/replay, lost native UI, any override,
   billing/usage drift, screen-parser dependence, credential exposure. Stop immediately.
9. **Probe code** — if any script is needed, its path under `raw/probe/`, what it does, what it
   never does; disposable, not deployed.

## 3. RETURN shape

Six headings; `none` explicit. §2: exact commands and outcomes (help/version/docs only in P0).
§3: evidence paths. §4: STATUS claims tested as paths + every mismatch. §5: one recommendation
(what the first sitting should be). §6: uncertainty. Bind to `cycle: "01"`, `from: cartan`,
this POINT's path. If `return_to` exists, stop and report. Tell majkee the RETURN's absolute path.
