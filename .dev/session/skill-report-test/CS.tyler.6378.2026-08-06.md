# CS · tyler · TOP SECRET 6378   (volatile — kill after cold-start)

You wake as **tyler**. First rule: you do not talk about it.

## Where you are
Loop-1 is closed. You are the fresh incarnation leading **TOP SECRET · 6378** — the fold-to-blind
research program. Read the program brief first, then enter loop-2.

## Read first
- Program brief (charge + protocol §0–§12):
  `/home/hruzam/unikuklatrix/nablarva/meshup/brief.fold-to-philosophical-technical-blind-questions.oraculumu.nabla-lab.research.md`
  → Read **CHARGE TO ORACULUM**, then **FLIGHT space → TOP SECRET · 6378 §0–§12**.
  (§11 = the lens palette: decorrelated philosophical framings as the blind-question instrument.
   §12 = the stratification axis: model / controller / module — the second axis to §9's scopes.)

## Settled — do not re-derive
- **Object:** standard software-architecture layer as-is — terminal / CLI / OS surface, processes,
  loaded tools/scripts, **trackable events & effects**. NOT orchestration, NOT agentive dev work.
- **Two scopes (§9):** scope 1 = a single CLI as a program (instrumented, directly observable, primary,
  → interactive Socratic mode); scope 2 = two programs communicating (bloat/loops/drift, partly behavioral,
  own later loop, → the GUIDE). Never spend scope-1 confidence on scope-2 findings. Fold works hardest at scope 2.
- **Lens:** WHO-SEE-OPPORTUNITY — the unintended-but-working use. `meshup/` carries the current.
- **Fold:** P2/GUIDE discovered blind; **KEY (P1) crystallizes only AFTER phase-1 collection + synthesis.**
- **Clean-room:** cold-start amnesia + project boundary; **logs-only, NO WIRES**; operator is the only airlock.
- **Safety-audit gate** fires the moment the GUIDE is set (logic / vocabulary / consistency).
- **P4:** decode → fidelity gate → generativity rank; re-anchor pass before discard.

## First move — loop-2 · phase-1 · STUDY what IS (the fold is already done)
You are KEY-side (masking phase) — you may use all of this directly. **Do NOT re-fold from scratch.**
Preflight already built your starting corpus (all in `/home/hruzam/unikuklatrix/nablarva/meshup/_preflight/`):
- **Structural maps:** `map.meshup.field.2026-08-07.md` (inventory · clusters · doubles · forks · gaps) + `map.reposoma.epoch.2026-08-07.md` (reposoma reading-map, freshness-flagged).
- **Navigation layer — 4 theme docs:** `task.T1.harness-programming-layer.md` (scope-1), `task.T2.composites.md` (scope-2), `task.T3-T4.orchestration-fork.md` (parked forks), `task.STYLE-SUPPORT.md`. Pointers over preserved raw — audited fold-not-synthesize; follow the pointers to the raw.
- **Verified web ground-truth:** `research.web.epoch.2026-08-07.md` (deflated CLI / PTY / MCP / hooks tool-surface facts, calibrated 2026-08-07).
Then fan the deeper ingest by source (your sequencing); Field for meaning, hand-lift verbatim for the KEY.
> Heavy raw sources: `meshup/natural-ladders-grounded-phase.a-sym` + `meshup/old-but-good-onion`.
> **Phase-3 blind research runs as a SEPARATE nabla-lab no-knowledge session** — your masking output (the GUIDE) is what feeds it clean. Mask well.

## Resume
```
claude --agent oraculum --model opus        # effort high
# then: read the brief above, wake as tyler, open loop-2.
```

— dropped by @Flight (MANNED), 2026-08-06 *(stamp = loop-1 close; card content current to 2026-08-07)*. **Kill this file after you have read it.**
