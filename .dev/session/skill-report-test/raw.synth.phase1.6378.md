# RAW SYNTH · phase-1 · 6378 (KEY-side working material)

> Gitignored, camouflaged working file. This is phase-1 COLLECTION (verbatim + meaning).
> **Phase-1 COMPLETE (2026-08-08). Crossed into phase-2 masking.**
> The KEY (P1) now lives in the safe: `nablarva.devenv/.hlm/cooking-recipes.yaml` (D-D).
> Cipher rows are NO LONGER duplicated here — the safe is single source of truth (airlock).

---

## SOURCES INGESTED

**scope-1 (meaning-layer complete):**
- **S1** · `old-but-good-onion/{terminal-onion-study, interposition-study}` — deflated pole: terminal rings, PTY wire,
  `/proc/PID/fd`, JSONL append-only truth, `pipe-pane`, OSC-133, adapter/spine/seam/canary.
- **04_** · `oraculum-basic-triangulation/04_LABORATORY_OBSERVABILITY_AND_EXPERIMENTS.md` — scope-1 lab spec, deflated:
  event vocab (§4.13), observable-layers stack (§4.3 ≙ §12), epistemic ladder (§4.6 ≙ §9).
- **S2** · `natural-ladders/{Houston bonding-layer, asymmetry codex bonding-layer}` — @field mechanism inventory
  (hooks/skills/MCP/sandbox/approval-policy/profiles/.rules/JSONL-stream/SDK/app-server) + Houston §7 LARVA→CLI table.

**scope-2 (COMPLETE — 2026-08-08):**
- `grounded-composites/costa.seed.codex-claude-composite.2026-08-07.md` — point-to-point synchronous bridge.
- `oraculum-basic-triangulation/01_ARCHITECTURE_ROOM_AND_BROKER.md` — brokered N-peer room (larvad).
- **S3** · `grounded-composites/assymetry-preConsultation.md` (~1537 ln) — the only source with *observed working shell*
  (`claude -p`, `tmux send-keys` doorbell, `.agent-room/` file protocol, atomic rename-publish, Stop-hook completion,
  blocking wrapper with `noclobber` lock + timeout poll). Deflated natively — no agentive residue, no classifier risk.

Ground-truth: `_preflight/research.web.epoch.2026-08-07.md` + **delta pass `research.web.epoch.delta.2026-08-08.md` (@Epoch, G1 — running).**

---

## FINDINGS (do not lose)

### F-01 · Claude↔Codex asymmetry is STRUCTURAL, not naming
Claude keeps control in-session (skill-scoped hooks, `!command`, `context:fork`, `defer`/resume); Codex exposes an external
control plane (`codex exec --json`, TS SDK, MCP-server, JSON-RPC app server). §12: Codex externalizes at controller/module,
Claude internalizes. The Codex external plane is a scope-1 observable that IS the bridge to scope-2. S3 reconfirms: Codex
has no native heterogeneous-provider subagent primitive → "consultation," not "spawning."

### F-02 · Negative-space: no `SkillActivated` event (either platform) — P3 seed
"A process loads instruction-units on demand but fires no event when it does — how do you prove activation?" (Houston §5.5).
Gate a consultation by hooking the tool the skill uses (`PreToolUse`), not the skill. Corollary: activation is SILENT but
completion is LOUD (`Stop` hook fires reliably). The silent-activation / loud-completion asymmetry is itself a P3 seed.

### F-03 · Corpus solves all three §9 scope-2 pathologies STRUCTURALLY (Y/broker)
bloat → sparse cursor projection (§1.7); runaway loops → consultation lease/budget (§1.8); drift → versioned goal +
human-accepted checkpoint (§1.8). Structural enforcement, not cooperative convention. → forecast FC-scope2-structural.

### F-04 · Both F1 branches grounded — but the OBSERVED-SHELL branch is X, not Y
room-A/Y (broker) = DESIGN; room-B/X (file-room: `.agent-room/`, atomic `mv`, `.done`, `noclobber`, timeout, tmux doorbell)
= OBSERVED WORKING SHELL. The run-evidence is on X; the better-architected Y is still aspiration. Direct input to
gavel-docket 1/4/6. F1 genuinely live: X is run, Y is drawn. (Evidence-grade recorded per-branch in the KEY — G2.)

### F-05 · Pre-registration seed (D-C) for the scope-2 callable question → forecast FC-scope2-callable
Contaminated-frame answer: blocking wrapper → request file → tmux doorbell → living B answers → Stop-hook captures →
atomic rename-publish → `.done` → wrapper returns stdout → caller continues. Spine: *terminal UI ephemeral; file room durable.*

### F-06 · S3's three-layer cut — candidate GUIDE scope line (deferred, G4)
transport (dumb: files/tmux/sockets/MCP) · lifecycle (who's alive) · cognitive contract (what's asked — scope-independent).
scope-2 lives in transport+lifecycle. Cleaner than §9's program-count axis. Adopt at GUIDE authoring.

---

## LOCKED DECISIONS

- **D-A** (2026-08-07): rich KEY → stripped GUIDE. KEY private, GUIDE shareable.
- **D-B** (2026-08-07): cipher-the-fork — forked story-term maps to BOTH plain terms; double-map intentional.
- **D-C** (2026-08-07): pre-register the contaminated-frame forecast, seal with KEY, score P3 against it in P4. Palette
  judged by fidelity-survival; forked-term fidelity per-branch. Seeds: FC-scope1-baseline, FC-scope2-structural, FC-scope2-callable.
- **D-D** (2026-08-08): **T-safe = `nablarva.devenv/.hlm/cooking-recipes.yaml`** — synced git-home, off-wire (tailscale
  mesh), crash-durable. Only exit is the stripped GUIDE → `~/nabla-lab/drafts/` at phase-3 release.

## GAVEL-LOOP RESOLUTION (2026-08-08)

- **G1** version-volatile rows → @Epoch verification pass AUTHORIZED + running; rows quarantined `evidence: pending_epoch`.
- **G2** evidence-grade per fork-branch → applied in KEY (room-B observed / room-A design).
- **G3** F1 itself → **REFUSED / stays ungaveled** through phase-2 (cipher-the-fork carries both; blind phase + majkee decide).
- **G4** T-scope-line (F-06 cut) → DEFERRED to GUIDE step; lean recorded.
- **G5** pre-registration granularity → coarse per-scope (3 forecasts in KEY).
- **G6** KEY schema → LOCKED as YAML (Oraculum holds pen, majkee reviews).
- **G7** T3-T4 orchestration + STYLE/lens-palette → SKIP, stay parked (out of KEY).

## OPEN THREADS

- **T-scope-line** (F-06): candidate cut chosen; formal adoption at GUIDE authoring (phase-3 setup).
- **T-epoch-delta** (G1): @Epoch running — on return, promote verified rows / convert unverifiable to GUIDE negative-space questions.
- **T-safe**: ✅ RESOLVED → D-D.
- **T-assymetry**: ✅ RESOLVED → S3 ingested.

## KEY-CANDIDATES

> **Folded into the safe** `nablarva.devenv/.hlm/cooking-recipes.yaml` (23 entries + 3 forecasts).
> No longer duplicated here — single source of truth, airlock discipline.
