# nabLarva seed — distilled from notebook pages 2026-07-30 (2:26 AM)

> Author: Oraculum, 2026-07-31. Source: six photographed notebook pages,
> buffered in a remote loop. Verdict delivered before reading any project file
> (cold-eyes rule respected). This is the canonical transcription-synthesis.

## Governing insight

larva V3 is the moment the larvaTnux experiment stops being an experiment and
becomes a product with a social layer. Two organisms share the notebook:

- **Cluster A — mechanism** (stays in `applications-in-common`):
  session clipper / processor / composer machinery.
- **Cluster B — product + strategy** (lives HERE until it graduates):
  ∇larva identity, multi-vendor onboarding, human-team hub → stridularium.

Bridge: the clipper's `OUTPUT (A)` is the feed unit the stridularium would
distribute. One boundary note must own that interface once both threads run.

## Cluster A — session clipper (mechanism, playground-bound)

Pipeline: START → SESSION → OUTPUT → FILTER {noise (*), ~~vendor~~,
process-phases, spawn-report} → OUTPUT (A).

- **HUB** + session details; session tools / translators:
  - metadata → load files (protocol, templates); inform agent what is happening
  - **glue document** — `/clear` OR restart session:
    - where was last prompt → AGENT MEMORY
    - where is glue document → INCARNATION
  - feedback; debug process display (*)
- **Strong commands** (syst. operability triggers): kill window, session
  restart, session sleep, hub restart, new terminal session.
- Research margins: meditation · incarnation · personal data.

Processor page (SESSION | CLIPPER | PROCESSOR):

- OUTPUT (A) → **BUFFER** → **COMPOSER** → INPUT `<entity><entity>` blocks
- BLOCK → ROLLER: dialog-all · ROLLER: private
- **TRANSLATOR** = smart compressor ← METADATA ← **ANCHORS**
  (from-where-to-read, cached)
- CACHE(A): master prompt · info-for-solve
- SYSTEM FEEDBACK (frontmatters → in) (***)
- COMPOSER = most hardcoding; communication via SUBL socket
- Monitor: SUBLIME / ZED — **full reading for user only; agents get anchors**

## Cluster B — nabLarva product + Quo Vadis (this provisorium)

Identity: logo `larva∫ux` / `∇ larva` (nabla larva) · brand-car project:
**larva V3**.

Protocoles page:
- agent delegating task to some tasks
- FLAT → all equal, common work
- blind triangulation
- loop: project architect → majkee + tester agent (sequential), filling to
  protocole (feedback arrow back)
- SPECIFIC TAG → only-for (private conv.) · saturate-up in mass conversation
- future commands → when sent → composer recognizes
- "commit philosophy is nice light" · dialogue room repo?

∇larva features (page 1):
- script release / submit for (Claude + similar)
- agents fill options for colleagues — each option dedicated to a separate
  receiver; like plan-mode submits over sessions
- user sees fill-ins from agents the same way agents receive from user
- ∇larva consumes such files from UI, or system recognizes interactive prompts
- layers ≈ slash options → own channel
- still need a specific UI (light panel → terminal wrapper)

Future needs (page 2):
- codex (CLI) on board · gemini CLI tokens still alive (unknown horizon) ·
  cursor IDE/CLI as small synchro for harness tests + colleague knowledge
- piql — living machine (office). HOSTING MACHINE? **"Do we really need a
  living machine? — give me blind pros vs against"** → vega pass (position-free)
- QUO VADIS MAJKEE: hub for my human team with their AI, where dev work can be
  planned — with architect agents; plan tasks

Topology (page 3):
- one specifically-designed central repo; each human can input his chat to the
  central repo and assign a specific participant; private message to a specific
  participant same as send
- Tailscale? — BUT: app itself holding restrictions per hosting computer
- +1 goal or ver **V1 — sweet spot**
- WHAT IS PENDING:
  - AGENT(home) <> AGENT(office) — Tailscale?
  - AGENT(host) <> AGENT(host) — larvaTnux
  - SUBLIME or ZED (display) compatible → set of bricks
  - PROCESSOR: terminal (controls · commands) → still sure 'composer' not
    needed?

## The ladder (locked framing, majkee 2026-07-31)

1. agent <> agent — same host (larvaTnux bus)
2. agent <> agent — different host (transport TBD)
3. **stridularium** — regulated Discord-like block for humans + their AI
   collaborators; planning and meeting projects. Quo Vadis stage.

## Open decisions (pending underlines — none locked)

| # | Question | Suggested seat |
|---|---|---|
| 1 | Living/hosting machine: blind pros vs against | @vega (position-free brief) |
| 2 | Composer: needed at all, or terminal processor suffices? | hypothesis in playground first |
| 3 | Cross-host transport: Tailscale vs app-held restrictions | research pass (@epoch) before locking |
| 4 | Monitor surface: Sublime vs Zed — brick-set compatibility | playground experiment |
| 5 | Dialogue-room repo: separate repo per room, or central repo + tags? | strategy, here |
| 6 | V1 sweet-spot cut: which ladder stage is V1? | majkee gavel |
| 7 | Bridge note owner: clipper OUTPUT(A) ↔ stridularium feed interface | decide when both threads live |

## What I will not decide

- Graduation target of this provisorium (project vs temple scope) — majkee.
- Any mechanism hypothesis outcome — belongs to the playground loop.
