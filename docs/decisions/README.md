# decisions/ — ADR dir (append-only)

Empty until the first post-gavel decision. Format: `NNNN-<slug>.md`, one decision per
file, never rewritten — superseded ADRs get a pointer, not an edit. The founding docket
lives in `../../session/flag.md` until gaveled; gaveled items land here as ADRs.
