---
goal: Choose a build-ready first Nablarva increment that reduces human session relay while preserving independent seats and human authority.
gate: Majkee records GO or STOP on Oraculum's evidence-backed audit of Cartan's first-brick Nablarva design and acceptance test.
participant_1: [oraculum, {brand: Claude Code CLI, model: Fable, effort: operator-selected}, hruzam-120922]
participant_2: [cartan, {brand: Codex, model: Astra, effort: session-configured}, hruzam-120922]
participant_3: [majkee, {brand: human, model: not-applicable, effort: not-applicable}, hruzam-120922]
status_owner: oraculum
standing_witness: "cartan — head coordination and STATUS claims, never acceptance of Cartan's own design"
head_note: "cSharp — Oraculum authors the adopted RUNBOOK and stays live through the whole arc as navigator and status_owner; it may delegate every body of work, receive navigation and test parts, and close the session if it can."
prepared_by: cartan
prepared_on: 2026-09-12
workspace: /home/hruzam/unikuklatrix/nablarva
---

# Nablarva — first-brick design

This launcher is a Cartan-prepared candidate for Oraculum to review and adopt, not a
claim that Oraculum has already read, accepted, or started it. The holding copy is
`/home/hruzam/unikuklatrix/nablarva/meshup/nablarva-01-design/`.
Placement exception: the usual reposoma drafting-bench write failed at the filesystem
boundary. This workspace-local substrate copy is not a new standing bench or live session.
Its intended live home is
`/home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/`.

Review and resolve participant instrument/host details before promotion. The model labels
come from majkee's allocation, not capability measurements or executable model IDs.
Promotion makes the launcher live; thereafter it is fixed. STATUS alone carries position.

## Scope and gate

Design one same-host, one-bed extension starting from the existing runbook visual helper:
operator-selected full sessions attached to the run's head, explicit endpoint bindings,
and a graduated path from preparation to one demonstrated request/response pipe.
Nablarva is the product; this does not name a second relay product or decide a semi-IDE.

The deliverable is Cartan's `design.md`, including the first increment's acceptance test,
with a checked RETURN and Oraculum's independent VERDICT. The gate is a single human
decision on that package. REVISE continues this gate; GO or STOP closes it once recorded
against the exact reviewed revision. GO requires material lock conflicts and prerequisite
holds to be resolved or the dependent mechanism excluded. GO approves the bounded design,
not execution of a relay test or deployment.

No application implementation or live-send experiment belongs to this design gate.
A build is a later, explicitly authorized sibling with its own observable gate.
Apply the native/reuse check inline for this bounded extension; if it grows into a new
standalone system, stop and apply the big-scope research rule rather than widening this gate.

## Shared rules and ownership

- Oraculum is first in, last out: navigator, sole STATUS owner, and auditor of Cartan's
  technical work. Cartan is the primary technical designer and subsequent engineering
  integration owner, not a compatibility reviewer.
- Cheap native helpers may own bounded read work, scratch drafts, and clerical preparation.
  Oraculum checks their substance and publishes its own POINTs, VERDICTs, and STATUS.
  A helper cannot independently advance STATUS or impersonate either full-session seat.
  Delegation briefs name exact paths and forbid overwriting other participants' work.
- Cartan may use its native bounded helpers; Cartan integrates and checks their claims.
  Neither runtime's child is a substitute for the other independent full CLI session.
  Full-session exchanges use BUS; do not create one BUS cycle per internal helper or keystroke.
- Oraculum audits without fixing Cartan's artifact. Revisions return to Cartan through a new
  cycle. Cartan witnesses head coordination/STATUS claims, not its own technical acceptance.
  If Oraculum substantially co-authors the technical body, a named uninvolved reviewer must
  witness it before acceptance; record the independence limit, never self-confirm.
- Majkee carries paths between full sessions and holds meaningful architectural gavels.
  No automatic wake, live steering, or background watcher is authorized by this launcher.
- Follow the shared RUNBOOK, cSharp, STATUS, and BUS guides below. Use the fan-out chapter
  only if independent full-session branches are actually opened together. One STATUS,
  content-sensitive recovery, immutable handed-off receipts, no fourth BUS kind.

## Known holds and write boundaries

- Only session documents in the live bed and its bounded pulse router entry are in the
  activation scope. Oraculum owns RUNBOOK adoption, STATUS, router maintenance and its BUS
  receipts; Cartan owns the technical design and its RETURNs. Other evidence/scratch files
  need a named owner. Never create an empty BUS or a second live plan/pulse.
- `/home/hruzam/ia-sync/zsh/session/runbook.py` and its current owner bed
  `/home/hruzam/ia-sync/.dev/session/runbook-tool-00/` are read-only references here.
  Coordinate extension timing with majkee and Trajectory before any future source change.
  Trajectory is a consulted tool owner, not an already-woken participant in this RUNBOOK.
- The project flag, especially L3/L4/L6/L9′/L12 and pending docket choices, remains binding.
  The September send-keys proposals conflict with L4; naming a candidate does not lift it.
  Cite the exact affected lock and request a gavel before selecting an incompatible mechanism.
- Do not treat BUS as a replacement for the locked room journal. Map the task-evidence and
  future room-state layers explicitly. The browser's existing parsing is display-grade.
- No terminal injection, launch, remote transport, runtime/config installation, deployment,
  authentication access, repository cleanup, commit, push, or prune is authorized here.
  Do not read `/home/hruzam/unikuklatrix/nablarva/.hlm/`.
  Preserve other sessions' dirt and sealed/blind work; read only the named relevant evidence.
- Higher-mathematics research, global role identity, unattended operation, cross-host delivery,
  and a general IDE/orchestrator are outside this first gate. Do not reopen them by analogy.
  Do not silently move interactive work onto an API-billed path.

## prompt-0 — Oraculum / cSharp

You are `oraculum`, the independent full Claude Code CLI session majkee selected.
Read, in order:

1. `/home/hruzam/unikuklatrix/nablarva/AGENTS.md`
2. `/home/hruzam/unikuklatrix/nablarva/.dev/session/flag.md`
3. `/home/hruzam/unikuklatrix/nablarva/.dev/session/pulse.md`
4. `/home/hruzam/unikuklatrix/nablarva/PROJECT.yaml`
5. At bootstrap, `/home/hruzam/unikuklatrix/nablarva/meshup/nablarva-01-design/RUNBOOK.md`
   and `/home/hruzam/unikuklatrix/nablarva/meshup/nablarva-01-design/brief.md`; then the
   head's reading route in that brief. On recovery, instead use the live RUNBOOK, brief
   and STATUS under `/home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/`.
6. The shared guides listed below; inspect any applicable local instructions before writing.

Study `/home/hruzam/unikuklatrix/nablarva/meshup/` using brief.md's historical reading map.
It is background, not a live plan, current vendor manual, or permission to execute old probes.
Use it to understand inherited thinking and sharpen your later audit. The main technical
design comes from this existing Cartan session; do not commission a competing design from
the archive. Bounded cheap reading helpers are welcome, with checked source pointers.
Exhaustive archival reading is not a prerequisite to opening Cartan's first bounded POINT.

At first activation, review this candidate and author the adopted RUNBOOK and brief in
`/home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/`.
Create initial STATUS there using the STATUS guide's mandatory fields and the gate verbatim.
Register only slug, gate, and exact STATUS path in
`/home/hruzam/unikuklatrix/nablarva/.dev/session/pulse.md`.
Do not overwrite an existing live bed: inspect and recover its current position instead.
Do not promote if the proposed gate conflicts with majkee's current direction; surface that
specific issue first. The holding copy never becomes a second state store.

Delegate one coherent first POINT to the existing `cartan` full session: technical design,
native/reuse evidence, and a predeclared acceptance test for the smallest useful increment.
Name the exact design/RETURN write paths, prohibited operations, and done-when.
Write the POINT and STATUS before giving majkee one absolute path to carry.
Do not spawn a new Cartan in place of the existing session.

On RETURN, inspect the actual proposal and cited evidence; obtain focused audit help where
useful, and publish your independent VERDICT. Do not repair the design yourself.
Keep STATUS a short recovery snapshot, with one next action and no history shelf.
Reconcile artifact contents before any resumed dispatch; unknown work is not permission to
repeat it. Have Cartan check your coordination/STATUS claims separately from design acceptance.

When the package is ready, ask majkee for GO or STOP and record the exact decision and
reviewed revision. A chat response is a human decision to preserve, not a fabricated BUS
receipt from another seat. Stay through closeout: preserve keepers and a short experience
transfer; request any additional promotion/commit/prune authority rather than assuming it.

## prompt-1 — Cartan / technical lead

You are `cartan`, majkee's existing independent Codex session, not Oraculum's child.
Read the project chain listed in prompt-0, then:

- `/home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/RUNBOOK.md`
- `/home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/STATUS.md`
- `/home/hruzam/unikuklatrix/nablarva/.dev/session/nablarva-01-design/brief.md`
- The current POINT addressed to `cartan`, carried by majkee, and its cited substrate.

Resolve the declared seat before work; a mismatched `to:` or `return_to:` stops the cycle.
Write the assigned design and your own six-field RETURN; never rewrite Oraculum's STATUS.
Make substantive design decisions and reasoned recommendations here, using bounded native
helpers where useful. Preserve disagreements and distinguish observed facts from proposals.

The design must make the UI/attachment/transport boundary concrete without creating unnecessary
products or frameworks. Cover operator binding and explicit replacement/rebinding; host-local
endpoint state versus portable task truth; exact artifact/mission correlation; uncertainty
after submission; manual fallback; and the native change that would retire the delta.
Do not use a title, board record, file appearance, or terminal silence as authority.

Include a future trial against the current manual workflow, with observable acceptance and
stop conditions agreed before execution. Count actual operator interventions; test wrong
bed/seat, stale/replaced endpoint, busy/approval state, and interruption after possible
submission. Submission, correlated response, witness acceptance and STATUS advance are
distinct. No silent retargeting or blind retry. This gate specifies that trial; it does not run it.

Refresh volatile runtime claims through the installed interface and current official sources
before relying on them. Neither old reports nor a demonstrated launch proves a working
cross-vendor relay. Keep the proposed first build bounded enough for independent audit.

## References — shared law

- `/home/hruzam/reposoma/raw.guides/runbook/GUIDE.md` — one gate; drafting bench and promotion.
- `/home/hruzam/reposoma/raw.guides/runbook/res/csharp-head-protocol.md` — first/last, delegated bodies, crossed witnesses.
- `/home/hruzam/reposoma/raw.guides/status/GUIDE.md` — bounded snapshot and read-only recovery.
- `/home/hruzam/reposoma/raw.guides/bus/GUIDE.md` — ownership, six-field RETURN and independent verification.
- `/home/hruzam/reposoma/raw.guides/runbook/res/fanout-turns.md` — only for full-session fan-out.
- `/home/hruzam/reposoma/raw.guides/runbook/res/research.md` — small-extension versus big-scope boundary.
- `/home/hruzam/reposoma/raw.guides/runbook/res/presence-board.md` — advisory boundary, not runtime addressing.

The token-economy chapter remains DRAFT. Helper use here follows majkee's explicit allocation,
not an assumed gavel of that chapter or an unmeasured model-cost comparison.
