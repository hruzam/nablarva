# Cartan — reading notepad for the tmux restart

Captured 2026-09-12, office, at majkee's request. This is a retrieval aid for the same
engineering session, not canon, STATUS, a second plan, or an instruction to execute old
research. Read the draft brief for the shared synthesis; use this file for depth that would
otherwise be lost at restart. Once Oraculum opens the live bed, its STATUS owns position.

## The human agreement to preserve

- The whole product is Nablarva. Majkee asked for a lot of reading first, then a graduated
  design. Vendor changes are weather; a semi-IDE is not an assumed destination.
- The first useful brick may extend the runbook visual helper. Majkee specifically wants
  pipes/attachments from selected full sessions to a head, not merely better clipboard text.
  Context-copy remains a possible step, not a reason to discard that target.
- Final allocation: Oraculum/Fable is cSharp, first arriving and last leaving, sole STATUS
  owner, using cheap bounded helpers for drafting/preparation. This Cartan/Astra session is
  the main technical designer and engineering/integration owner, with its native helpers.
  Oraculum audits Cartan's work. Majkee holds architectural gavels and initially carries paths.
  There is no third standing BUS clerk. The earlier Sonnet/Terra-head proposal was superseded.
- Independent full CLI participants are not native children. Do not replace this Cartan
  with a spawned child named Cartan or Astrobley. Cheap drafting does not transfer authority.
- Majkee invited Oraculum to read meshup as historical background, not to take over the main
  design. The study route now lives in brief.md. No competing design from the archive.
- Higher-math research is unfinished and explicitly parked. Do not study/gavel it as a
  prerequisite. Future practical human-relay examples may still arrive.
- The operator will stop this client and resume it inside tmux. Saving continuity is
  authorized; creating/attaching tmux sessions or sending into other terminals is not requested.

## Checkpoint observed before restart

- Workspace: `/home/hruzam/unikuklatrix/nablarva`; office `hruzam-120922`; branch `core`,
  HEAD `b99701f4b04735a16cb51257d070ba91b01a96b6`, upstream `origin/core`.
- Pre-existing untracked paths: `.dev/session/runbook-tool-00/` (a MOVED stub) and `_mail/`.
  Preserve them. No unrelated work was cleaned, committed, staged, deployed or transported.
- New task documents are under
  `/home/hruzam/unikuklatrix/nablarva/meshup/nablarva-01-design/`.
  RUNBOOK.md is a candidate; brief.md is buffered inheritance; this file is Cartan's notepad.
- The attempted canonical reposoma RUNBOOK-bench write failed before creating its target.
  The local holding location is explicitly disclosed, not a new standing bench convention.
- No live `.dev/session/nablarva-01-design/` existed at capture. Pulse still routed no active
  session; flag, pulse and PROJECT were unchanged. No POINT, RETURN, VERDICT, code, or live
  relay was authored/executed for the new design gate. If that changes during the restart,
  inspect the new live artifacts; never initialize over Oraculum's work.
- The draft's first gate is design-only: majkee records GO or STOP on Oraculum's checked
  audit of Cartan's first-brick design and acceptance test. This is not build/send approval.
- Main checks parsed launcher YAML, verified unique seats and source paths, and confirmed
  the absence of live-state changes. A bounded native verifier found no launch blockers;
  it was a draft check, not Oraculum's future technical audit or an acceptance of the design.
- No active tool goal exists. No external participant was contacted or woken. One native
  read-only draft reviewer completed; it did not write files or replace Oraculum.

## Source hierarchy and conflicts already resolved

The mandatory local chain is AGENTS → `.dev/session/flag.md` → `.dev/session/pulse.md`
→ PROJECT. L9′ supersedes the old pulse-as-doing-state text: pulse is a bounded router,
per-gate STATUS is the sole live position. L12 supersedes old separate-repository topology:
Nablarva, its former devenv and Termbrana are unified. Never rebase `core`; parallel code
uses worktrees. `.hlm/` is sealed and was not read.

L3's room journal/projections/structural regulation, L4's recorded deaths, L6's reviewed
promotion boundary and L8's Cluster A exclusion still hold. BUS task receipts do not silently
replace the room journal. Termbrana's Rust constraint does not decide Nablarva's language;
PROJECT still says TBD. No new lock was gaveled by our reading or the draft launcher.

The September briefs propose send-keys as an actuator while L4 rejects injection. Keep this
as explicit curvature. Likewise the current cSharp operator-carrier clause is not already
permission for automatic full-session steering. A workable local demo would not amend either.

## Reading already completed — retrieve selectively, do not restart the whole survey

This session read many documents across these groups, not every file in meshup. Source paths
below are retrieval routes; they are not orders to run embedded commands or current API facts.

### Closest practical seam

All three files in
`/home/hruzam/unikuklatrix/nablarva/meshup/a-symmetry-lightest/` were read fully.
The original Symmetry brief is corrected by Asymmetry's longer consultation and the later
countersign: endpoint, not leader; activate/observe/signal, not a UI-derived collect;
files carry truth, mission is a pointer, native CLI remains accessible, interactive usage
must not silently become API usage. Purpose-reminding composer work was rejected for this
build. Unattended policy must be earned, not assumed.

The useful kill test is maintenance: if ordinary safe operation depends on a continuously
updated vendor UI/prompt parser or terminal emulation, stop/re-scope. Count actual manual
relays and failure modes before claiming savings. No current vendor claim in those briefs
was independently refreshed through official web documentation during this intake.

### Earlier mental phases

- `/home/hruzam/unikuklatrix/nablarva/meshup/oraculum-basic-triangulation/`:
  seed, vision, comparison, handoff, and chapters 01–07 were read. Task/goal versioning,
  leases/budgets and independent/reveal/challenge/closure phases inform the discipline;
  they do not impose a generalized driller on this first brick. Current flag wins.
- `/home/hruzam/unikuklatrix/nablarva/meshup/nabla-buffer-brideAndBook/braid-and-book.substrate.2026-08-01.md`:
  raw braid versus derived book, typed claims/provenance, independent read sets, one writer.
  Do not erase chronology just because a derived synthesis reads more cleanly.
- `/home/hruzam/unikuklatrix/nablarva/meshup/symetry.claude.ai.claude-fable-5/blind-traingulation.chatgpt-asymmetry-sol.entity/`:
  the historical nine-document entity discussion was read. Portable enacted discipline,
  independence versus contamination, and vendor-neutral authority survive. Ontology-driven
  middleware and the math/swarm branch are parked, not a build mandate. This is an old
  historical corpus, not permission to inspect unrelated currently blind trials.
- `/home/hruzam/unikuklatrix/nablarva/meshup/old-but-good-onion/`:
  terminal and interposition studies were read. Keep CLI, PTY/rendering and disk planes
  distinct; disposable adapters and canaries are ideas, dated legal/price claims are not facts.
- `/home/hruzam/unikuklatrix/nablarva/meshup/natural-ladders-grounded-phase.a-sym/`:
  both large bonding-layer studies were read. Shared meaning is not identical native
  mechanisms. Do not clone Claude's persona roster into Codex or promise semantic parity.
- `/home/hruzam/unikuklatrix/nablarva/meshup/grounded-composites/`:
  Asymmetry pre-consultation and Costa seed were read. Blocking wrapper/file wake examples
  and hook/active-ID hazards are experimental history; later-p and git-as-completion ideas
  are not adopted just because executable-looking material exists.

### Practical runbook experience — the highest-value predecessor

The preserved source is ia-sync commit
`e12be1f3a33798a26323b47d7a4bc17917c3474f`, verified locally reachable.
Under `.dev/session/runbook-upgrade/`, VERDICT.md, the Cartan experience transfer, both full
audits, and relevant cycle 23/24 receipts were read. Use git show if a live bed is later pruned.

- Actual disposition: STOP, limited delivery accepted, original producer gate unmet.
  The recommendation was SIMPLIFY, not proof that a rebuilt relay fixes model behavior.
- A bounded Astra/xhigh producer draft passed; an earlier Sol sample failed. Neither is
  general model reliability evidence or a live crash/restart test. Claude producer remained
  FAIL; its coached recovery diagnostic was only partial. Causal explanations are unproven.
- Cycle 23: the wrong root/child topology produced convincing artifacts, including a head
  verdict through the wrong actor. It was retained as inoperative; cycle 24 corrected the
  full-session boundary. Hashes show bytes, not the independent writer's identity.
- STATUS reached 501 lines; 108 receipts illustrate carriage/metadata cost, not a reason to
  discard ownership and independent verification. Avoid repeating every assurance in STATUS.
- Full audits were not blind. Their measurement claims must be read through the corrected
  VERDICT. Do not reuse the rejected human-relay/hour/token counts or infer savings from
  head-only telemetry and repeated cached context. No price comparison is established.

### Trajectory and the current visual helper

Current source: `/home/hruzam/ia-sync/zsh/session/runbook.py` (2007 lines at inspection).
It was inspected around discovery, STATUS display, cycle grouping, copying and board grammar.
`y` copies a path, `Y` exact content, `R` a resume line. None delivers into another agent.
Loose display parsing and board presence cannot authorize action.

Source and office deployed copy had equal SHA-256 on 2026-09-12:
`266dcb0d3e23c9b381a20c0970dd7a6f47b642ded1e07a67c08d07244138c9c9`.
This was a read-only byte check, not a new runtime test. No selftest/TUI was run in this arc.

The source owner bed moved from Nablarva to
`/home/hruzam/ia-sync/.dev/session/runbook-tool-00/`; the Nablarva path is a stub.
Its STATUS was read: d-done but open, examples on hold, home carry owed. September 6's freeze
is historical: September 9's presence-board gavel and office client build supersede it.
The guide is gaveled; advisory IDs remain record IDs, not runtime IDs. Refresh before editing.

Both September 12 Trajectory consults were read fully. The first is preserved in ia-sync
`df94847`; the addendum was on disk, not claimed committed. Important corrections are in
brief.md. The proposed prompt-line readiness heuristic was never tested here. The addendum's
launch/capture example is not evidence for an assignment/RETURN pipe in both directions.

`/home/hruzam/ia-sync/devices/_shared/termux/bin/agentive-send` was read fully: a short wrapper
around tmux send-keys/paste/list, without recipient binding, readiness or reconciliation.
No command from that wrapper or the historical consult was executed by this Cartan.

### Adjacent mechanisms — context, not dependencies to absorb

- `/home/hruzam/ia-sync/HANDSHAKE.md` was read fully. Its courtesy POINT is not BUS's
  bounded assignment. Disk availability is not consumption. Its old draft header is followed
  by an actual later countersign; read dated changes in order. It is not a watcher contract.
- `/home/hruzam/unikuklatrix/nablarva/toolbox/termbrana/research/evidence/t06-tunnel-v0-roundtrip.md`
  was read fully: a zero-turn birth failed, a fix and a stored-thread cross-process trial
  passed for a bounded pairing. Old app-server/tunnel code was only partially inspected.
  This does not prove present interactive CLI delivery or all four vendor pairings.
- The historical seam-probe report was read; cross-host UI capture was unreliable and its
  Stage 6 write trial was gated/not run. Do not resurrect it from a "next step" paragraph.
- Termbrana's readme/decisions/M0 probe were read: a sensing/toolbox brick, not a ready relay
  engine. Ommatermia's before/expected/after evidence boundary and Ovitmugen's advisory
  window metadata are relevant separations, not permission to merge those threads.
- The separate codex-identity-resolution proposal was read only for overlap: full-seat versus
  spawned-child posture/identity work, pending operator decisions at inspection; not an
  endpoint registry. Other incarnation work was mapped only for overlap and left independent.
  Do not import unrelated blind-test contents or fork a second global identity scheme.

## Shared procedure already consulted

The RUNBOOK, cSharp, fanout, STATUS, BUS and presence-board guides were read fully.
Use their source paths in RUNBOOK rather than a second copied law. Token-economy and
cross-vendor-seat extension chapters are drafts; do not silently gavel them. Native helper
delegation is operator-authorized here, not justified by an invented cost/reliability norm.

Guide, buffering and cross-runtime harness instructions were used to keep this a shared,
unpromoted handoff. No Codex global primitive, runtime config, role roster, or deployment
was changed. The cold-start-card procedure now supplies restart glue; it is not doing-state.

Do not repeat the whole survey after resume. Resolve the actual current frame and live
ownership, retrieve the disputed seam, and continue as the technical lead under the user's
current direction and any real POINT. The historical notepad cannot authorize that POINT.
