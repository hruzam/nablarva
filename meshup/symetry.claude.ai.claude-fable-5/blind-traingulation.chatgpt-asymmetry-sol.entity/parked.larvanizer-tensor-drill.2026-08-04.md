---
artifact: parked brief — larvanizer & tensor drill
date: 2026-08-04
thread: symmetry.entity-seed (fork parked OUT of it)
status: PARKED · semi-metaphor with mechanical residues · no build, no loop
grep-tags: #parked #larvanizer #tensor-drill #medium-side
---

# PARKED — larvanizer / tensor drilling machine

## Status of the idea
- **Kept as semi-metaphor:** tokens-as-drill striking through tensor(s) of
  unlimited rank; the composer that assembles mounts by contraction.
  Not doctrine, not architecture — an image holding a seat for a mechanism.
- **Kept as real (survives the metaphor's death):**
  larvanizer = medium-side, pre-mount STAGING organ. Raw stream → larvae +
  cut-log. Nothing silently deleted; staging, not cleaning. LARVA-owned.

## Underlined possible solutions (the residue worth keeping)
1. **T0 deterministic larvanization** — lemmatize/regex/lint tier, CPU-free,
   agentctl-verb territory, clock-driven batch, off the hot path.
2. **Cut-log as telemetry** — every removal recorded; the log builds the
   noise-class catalog that decides whether anything above T0 is ever earned.
3. **T1 local model (ollama)** — larval classification/embeddings; queue-first,
   no parallel slots until queue-wait telemetry demands; jobs routable to any
   idle mount-table host (pool over tunnel). T2/frontier never cleans.
4. **Mathematician's entry point** — the counting problem (larval
   distributions per source/session/time), strictly before any ML.
5. **Tensor-composer proper** (drill through knowledge tensor to compose
   mounts) — deepest shelf; unlocks only after counting discipline + the
   routing need materializes. Adjacent to seed §5, NOT merged.

## Wake conditions
- cut-log exists and shows T0 miss-classes → consider T1.
- registered routing/composition need from the graduation loop → reopen
  tensor-composer as its own study.

---
lineage: entity thread 2026-08-03/04 exchanges → THIS. Sibling shelf to
multi-agent-composition seed (still separate).
pending: none — parked clean. Reopen by wake condition or @majkee's call.
