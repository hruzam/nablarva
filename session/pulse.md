# pulse.md — nabLarva · volatile state (the ONLY canonical doing-state, flag L9)

`single canonical file — no pulse forks · newest on top · archive when heavy`

## 2026-08-02 · founding gavels (atlas-ui, office)

- **Founding set GAVELED** (majkee): AGENTS.md · PROJECT.yaml · flag L1–L10 · this
  pulse · docs/ · beacon · .gitignore. Docket + O1–O4 + untabled stay pending.
- Scope-group renamed → `~/unikuklatrix/` (L5 amended).
- plan.md abolished (L9, survey receipt); `dock.md` born as the uncanonical scratch.
- Temple pilot 3 rerouted → elements-factory experiment space (L10).

## Founding arc — remaining (temple plan of record: ia-sync/_staging/nablarva.founding-plan.md)

- [x] A · substrate pulled from home (9 files, 76K) — 2026-08-01
- [x] B · placement + harness bundle + Janus + gavel — 2026-08-02
- [x] C · `nablarva.devenv` sibling + synchro wiring — 2026-08-02 (@Trajectory; template:
      applications-in-common.devenv, flat layout, GEMINI.md in sync set, dual-key
      registry.json; sync verified byte-identical. Untested-yet paths: .claude/ .gemini/
      .mcp.json — exercise on first real content)
- [x] D · git ×2 + gh ×2 PRIVATE — 2026-08-02 (nablarva 70674e6 · nablarva.devenv
      f0bd85a, branch core)
- [ ] E · zsh wiring — THROUGH THE SURGICAL TABLE ONLY (majkee 2026-08-02): author in
      ~/ia-sync/zsh/, deploy.sh spreads to both machines; never direct to live
      ~/.config/zsh/. Design: nablarva-toolkit.zsh + nablarva-keyboard.zsh (toolkit
      sources its own keyboard at entry) + 3 surgical edits (config, switcher,
      temple-project-map). @Eagle confirmed fit + law. **RESHAPED per majkee
      2026-08-02: own scope folder (ai/-pattern), NOT the projects/ toolkit
      mechanism.** Table state: NEW `zsh/nablarva/{base,keyboard,nablarva}.zsh`
      (signpost · aliases-only panel · nab engine; silent-on-source contract held;
      partitions reserved for post-gavel organs — mostly-empty by design) + 2 edits
      (config.office.zsh: NAB vars + base.zsh hook beside ai's; temple-project-map:
      2 entries). project-switcher reverted untouched. Syntax + isolated smoke green.
      DEPLOY NOT RUN — awaits majkee (bash ~/ia-sync/deploy.sh). Home config.home.zsh
      = home-side edit when majkee wires home
- [ ] G · pads for manual testing (larvaTmux pattern via @Eagle) — when first needed
- [ ] H · philosophy layer: provenance headers on experimental builds (flag L6/L7)

## Open (next sessions)

- Gavel session: 7-item docket + O1–O4 (flag) — O1 rides with docket 4
- Stage-2 groundwork: cross-host read channel = headless `claude -p` / session JSONL,
  NOT tmux capture-pane (seam-probe). Stage 6 write-test awaits majkee's explicit go.
- Home copy: majkee re-clones via git by hand, then cleans `~/provisorium` there.
- GEMINI.md = retired Bluebottle stub — majkee's cleanup call.

## 2026-08-01 · founding (atlas-ui, office)

- Substrate pulled from home (9 files, 76K), placement first `~/kukla/` (later →
  unikuklatrix). Harness bundle drafted; @Janus challenge → O1–O4 absorbed into flag.
- Home copy abandoned in place; majkee re-clones home via git by hand later.
- Registry beacon parked in-repo (reposoma read-only under pending two-sided merge).
