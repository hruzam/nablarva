# flag.md — nabLarva locked decisions + pending-gavel docket

`status: GAVELED — founding set locked 2026-08-02 (majkee). Docket + O-items + untabled`
`remain PENDING as marked. @Janus challenged 2026-08-01 (O1–O4 catch absorbed).`
`law: locked items are not re-litigated without new evidence. Pending items block their`
`dependents until majkee gavels. Append-only.`

## Locked (carried from gaveled sources)

- **L1 · Naming** (majkee 2026-07-31): nabLarva = the whole wrapper; stridularium = a
  component organ (which one = docket 7). Public naming avoids the t-word ('n' rule).
- **L2 · The ladder** (majkee 2026-07-31): stage 1 same-host bus → stage 2 cross-host →
  stage 3 stridularium (Quo Vadis). Framing locked; stage content evolves.
- **L3 · Triangulation settled points S1–S8** (triple/double-blind convergence,
  `triad.comparison.2026-07-31.md`): append-only single-writer journal as sole room
  truth · per-participant cursor projections · private = in-ledger visibility boundary ·
  structural regulation over cooperative convention · adapter-owned PTY, tmux never the
  protocol · monitor = plain files in Sublime/Zed · cross-host = same protocol over SSH
  to one central broker · room state on neutral ground.
- **L4 · Recorded deaths** (with cause, same file): git-as-wire · tmux send-keys
  injection · cooperative anchors as regulation · C++/Go static binary as forced v1
  (survives only via docket 2 learning path).
- **L5 · Placement** (majkee 2026-08-01, name amended 2026-08-02): scope-group
  `~/unikuklatrix/` at home root (not ~/www — different domain than the web
  precedents). Pair: `nablarva/` + `nablarva.devenv/`. Canon amendment note parked
  for Houston's reconcile pass.
- **L6 · Experimental-bed role** (majkee 2026-08-01): new experimental agentive shapes
  build here; structural builds stay on the surgical table (`~/ia-sync/`). Promotion
  out of nablarva = reviewed merge, never auto-deploy.
- **L7 · Guardrail** (temple doctrine D2′): unchanged `.md` ≠ unchanged behavior; the
  model is a moving target.
- **L8 · Boundary**: Cluster A mechanism (clipper/processor/composer) stays in
  `applications-in-common`; only the OUTPUT(A) ↔ stridularium feed interface will be
  shared, owned by one boundary note when both threads run.
- **L9 · Session surfaces** (majkee 2026-08-02, on the 8-root survival survey): canon =
  `flag.md` (locks, append-only) + `pulse.md` (volatile doing-state, single canonical
  file — pulse forks reliably orphan one branch; survey receipt). No standing plan.md
  (zero survivors in 8 roots). `session/dock.md` = UNCANONICAL scratch pad — any seat
  writes side quests there without waiting for a blessing; prunable without ceremony;
  never citable as truth. Task-scoped work: `session/<topic>/` dirs, die with the task.
- **L10 · Temple pilots run in elements-factory** (majkee 2026-08-02): temple-infra
  experiments (routing A/B pilot 3 and kin) land in the factory's experiment space, not
  in this repo and not in a scope-group sibling — nablarva history stays pure animal.
  Pilot facts travel to the temple by mail, never by residing here.

## Named deferral thresholds (folded from abolished plan.md)

- Domain seat: earned when a build-task class repeats 3× (not before)
- Orchestrator/sub-team: earned at ≥2 concurrent build lanes
- Robust gate (agentctl-style): earned at ≥2 materialized surfaces

## Pending gavel (majkee — the docket, `triad.comparison.2026-07-31.md`)

1. Files-only control spike: build X-pure one-day control, or accept 2:1 and skip
2. v1 language: Python stdlib vs C++ (learning appetite) — only majkee weighs
3. Git residual seat: archival layer over room dir — keep or drop
4. Broker lifecycle: tmux-pane foreground process vs system service (v1 lean: pane)
5. Journal store: NDJSON now, SQLite import path later — confirm or contest
6. V1 sweet-spot cut: one machine · one room · one human · two agents · one
   consultation (Z E.2) — confirm
7. Component naming: larvad/larva vs stridulator/stridularium organ mapping

## Open — carried from seed, NOT covered by the triangulation

(the triad settled architecture only; these are majkee's substrate questions,
`seed.oraculum.2026-07-31.md` §Open decisions — @Janus catch, founding challenge)

- **O1 · Living/hosting machine** — blind pros-vs-against (seed §open-1, @vega
  position-free brief). Feeds docket 4 — decide together.
- **O2 · Cross-host transport** — Tailscale vs app-held per-host restrictions. S7
  settled the protocol (SSH to one broker), NOT the transport.
- **O3 · Room repo topology** — per-room repo vs central + tags. S8 settled "neutral
  ground" only.
- **O4 · Monitor brick-set** — Sublime vs Zed compatibility. S6 settled the class,
  not the pick.

(seed §open-2 — composer needed at all? — is Cluster A playground scope, see L8;
seed §open-6 = docket 6.)

## Untabled (block final forge)

- majkee's mathematical engine research (natural socket: broker filter/compressor layer)
- brick-factory stock relevant to adapters/monitor
- rough file lot from majkee (drops here during gavel process)
- seam-probe Stage 6 write-test: prepared, GATED on majkee's explicit "go"
  (`meshup/nabla_drafts/report.seam-probe.2026-08-01.md`)

## Out of scope

- Cluster A mechanism work (lives in applications-in-common)
- Any public namespace claims (domains, packages, GitHub org names) before clearance
- Auto-deploy of anything from this repo to live `~/.claude/` — always via surgical
  table review
