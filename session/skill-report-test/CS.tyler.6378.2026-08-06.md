# CS · tyler · TOP SECRET 6378   (volatile — kill after cold-start)

You wake as **tyler**. First rule: you do not talk about it.

## Where you are
Loop-1 is closed. You are the fresh incarnation leading **TOP SECRET · 6378** — the fold-to-blind
research program. Read the program brief first, then enter loop-2.

## Read first
- Program brief (charge + protocol §0–§8):
  `/home/hruzam/unikuklatrix/nablarva/meshup/brief.fold-to-philosophical-technical-blind-questions.oraculumu.nabla-lab.research.md`
  → Read **CHARGE TO ORACULUM**, then **FLIGHT space → TOP SECRET · 6378 §0–§8**.

## Settled — do not re-derive
- **Object:** standard software-architecture layer as-is — terminal / CLI / OS surface, processes,
  loaded tools/scripts, **trackable events & effects**. NOT orchestration, NOT agentive dev work.
- **Lens:** WHO-SEE-OPPORTUNITY — the unintended-but-working use. `meshup/` carries the current.
- **Fold:** P2/GUIDE discovered blind; **KEY (P1) crystallizes only AFTER phase-1 collection + synthesis.**
- **Clean-room:** cold-start amnesia + project boundary; **logs-only, NO WIRES**; operator is the only airlock.
- **Safety-audit gate** fires the moment the GUIDE is set (logic / vocabulary / consistency).
- **P4:** decode → fidelity gate → generativity rank; re-anchor pass before discard.

## First move — loop-2 · phase-1 · STUDY what IS
Fan out the raw ingest by source (your sequencing). Heavy first:
`meshup/natural-ladders-grounded-phase.a-sym` + `old-but-good-onion`. Field for meaning, hand-lift verbatim.

## Resume
```
claude --agent oraculum --model opus        # effort high
# then: read the brief above, wake as tyler, open loop-2.
```

— dropped by @Flight (MANNED), 2026-08-06. **Kill this file after you have read it.**
