# Triad comparison — X vs Y vs Z (scored, fixed for gavel)

> Oraculum, 2026-07-31. Inputs: X = majkee (room-is-a-file: roller, git-diff
> wire, cooperative anchors) · Y = Oraculum (room-is-a-process: stridulator
> router, structural regulation) — `vision.oraculum.Y.2026-07-31.md` ·
> Z = Codex blind return — `~/reposoma/_mail/flight/bt-from-codex.md`
> (answered the position-free brief; never saw X or Y).
> Purpose: docket for the nabla-lab gavel process. Append-only.

## Naming ruling (majkee, 2026-07-31)

- **nabLarva** = the whole app / process wrapper.
- **stridularium** = a partial tool inside it (component, not the whole).
- Z's proposed process names (`larvad`, `larva` CLI) fit the family; final
  component naming = gavel item 7.

## Verdict

Blind triangulation converged. Z independently produced a hybrid of X and Y.
The two-competing-builds plan is likely obsolete; the disagreement space
collapsed to the gavel items below.

## Settled by convergence (no further debate without new evidence)

| # | Point | Votes |
|---|---|---|
| S1 | Append-only, single-writer journal is the sole room truth (roller / journal / journal.ndjson) | X+Y+Z (triple-blind) |
| S2 | Per-participant projection: `seq > cursor AND P ∈ to AND from ≠ P` — never own output, never others' traffic, delta-reading via cursor (X's diff principle survives here, without git as wire) | X+Y+Z |
| S3 | Private = visibility boundary in-ledger, owner-auditable; not crypto secrecy | Y-lean + Z |
| S4 | Structural regulation beats cooperative convention; no probabilistic drift detectors | Y + Z |
| S5 | Adapter-owned PTY per agent session; tmux never the protocol (only optional host/observation) | Y + Z (kills early tmux send-keys idea) |
| S6 | Monitor = plain files in room dir, opened/tailed in Sublime/Zed; no browser, no Electron | X+Y+Z |
| S7 | Cross-host = same protocol over SSH to one central broker; no distributed consensus | Y + Z |
| S8 | Room state on neutral ground — outside both project repos | Z (uncontested; adopt) |

## Z's novel organs (adopt whole, neither X nor Y had them)

1. **Consultation leases** — bounded budget objects (e.g. MIG-004, budget 8);
   traffic held at exhaustion; human chooses extend / reframe / decide / close.
2. **Four-phase blind protocol** — independent positions → simultaneous
   reveal (no anchoring) → bounded challenge → forced position closure.
   The buffering-triad method institutionalized inside the tool.
3. **Versioned goal revisions** — every message tagged `goal_rev`; goal
   change pauses consultations until human carries them forward.
4. **Human-accepted checkpoints** as cheap re-entry boundaries; never an AI
   summary silently replacing history.
5. **At-least-once delivery with replay markers** (queued/injected/responded)
   for the PTY injection leg.

## Deaths (recorded with cause)

- **Git as wire / diff-as-message** (X): superseded by cursor projections —
  same principle, no polling, no commit choreography. Git may survive as
  archival layer only (gavel item 3).
- **tmux send-keys as injection leg** (early X refinement): coupling to
  screen rendering; cannot distinguish noise/final/interrupt (Z F.2).
- **Cooperative anchors as the regulation mechanism** (X): outvoted 2:1 —
  agents cannot drift on traffic never delivered.
- **C++/Go static binary as v1 language** (Y): outvoted by Python-stdlib-
  first, Rust-when-pain-demonstrated (Z E.1) — unless gavel item 2 chooses
  the learning path.

## Forecast scoring (pre-registered in Y file — honesty record)

Forecast: "hybrid — X's ledger as truth + Y-enforcement only at injection
leg." Result: directionally right (ledger won; structural enforcement won);
WRONG on scope — enforcement belongs at the whole routing layer, not just
injection. Cause: flinching from the lightness constraint. Logged.

## GAVEL DOCKET (majkee, nabla-lab side)

1. **Files-only control spike** — build X-pure as a one-day control
   experiment with pre-registered failure predictions (Z F.1), or accept the
   2:1 vote and skip.
2. **v1 language** — Python stdlib (delivery speed, zero deps) vs C++
   (majkee's stated learning appetite). Different goods; only majkee weighs.
3. **Git residual seat** — archival layer over room dir: keep or drop.
4. **Broker lifecycle** — tmux-pane foreground process (Z) vs system
   service; v1 lean: foreground pane.
5. **Journal store** — NDJSON now, SQLite import path later (Z F.3):
   confirm or contest.
6. **V1 sweet-spot cut** — confirm Z E.2 topology (one machine, one room,
   one human, two agents, one consultation) as V1.
7. **Component naming** — larvad/larva vs stridulator/stridularium mapping;
   which organ carries the stridularium name.

## Untabled (blocks final forge — do not close before these land)

- majkee's mathematical engine research (natural socket: broker
  filter/compressor/scheduler layer — could still bend the shape).
- brick-factory stock relevant to adapters/monitor.
- rough file lot from majkee (dropping here during gavel process).
